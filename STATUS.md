## Last session
2026-08-18, later (**Mark's traffic capture killed my own diagnosis, and exposed two instruments that could not answer the question they exist for → rc7, LIVE on the channel**) — Fahim forwarded a clean ChatGPT reconnection recorded with the client-traffic recorder running. Issues [#120](https://github.com/plugpressco/saddle/issues/120) and [#121](https://github.com/plugpressco/saddle/issues/121) filed and closed, PRs [#122](https://github.com/plugpressco/saddle/pull/122)/[#123](https://github.com/plugpressco/saddle/pull/123)/[#124](https://github.com/plugpressco/saddle/pull/124) merged, **603 tests** (was 596), 0 lint errors. **rc7 published to R2 and verified live.**

**Retraction, and it was mine.** Earlier today I told Fahim the MCP-Adapter shim chain (#111/#112) was Mark's bug. It is not. His own report says `Transport: Saddle built-in JSON-RPC` — he does not have that plugin, so that code path never runs on his site. rc6 was worth shipping and was never his fix. *(Second correction in one day: I also wrote here that rc5 was never published. It was — the live channel was serving it. Both errors came from asserting before checking, once against a stale git ref and once against a file I had not read.)*

**What his capture actually proves.** ChatGPT's client (`Python/3.12 aiohttp/3.13.5`) made exactly two requests, both **401 at the transport gate**, with no `initialize` and no `tools/list` anywhere in the window; the browser half of OAuth returned 200 throughout. So this is authentication, full stop — not discovery, schemas, tiers, or the handshake. ChatGPT saves the connector because the browser flow succeeded and shows zero actions because its client never got in.

**Two instruments, neither able to say why — both fixed.**
- **[#121](https://github.com/plugpressco/saddle/issues/121), the one that mattered.** `probe_headers()` sent ONE loopback and it carried Basic. Basic is the scheme that survives where others don't: Apache/LiteSpeed consume RFC 7617 natively into `PHP_AUTH_USER`, so it reaches PHP even where the raw header is never forwarded, and a WAF rule can target `Bearer` alone. **So `self_check()` could report `auth_header: ok` on a site refusing every OAuth client** — exactly "Claude works, ChatGPT 401s". Application Passwords carry Basic (Claude Code/Desktop, Cursor, VS Code); ChatGPT has no field for a custom header, so Bearer is its only path. *"Claude works, so the header is fine"* is the wrong inference and this function was licensing it. Now two probes, judged on the scheme that **arrived**, with `bearer_header`, a `bearer_header_stripped` status, `htaccess_fixable` extended (the rule always forwarded the whole header — it just was never offered), and its own UI copy, because telling someone their pasted-key apps are broken when they aren't sends them hunting the wrong thing.
- **[#120](https://github.com/plugpressco/saddle/issues/120), why his capture was mostly noise.** `targets_mcp()` was a bare `strpos` prefix test and the admin API shares `saddle/v1`, so `/saddle/v1/mcp-diagnostics` matched — and the panel polls it every 5s while recording. At 25 entries the buffer turned over every **~125 seconds**; his window was **114**, and 23 of 25 rows were the panel watching itself. The instrument was evicting its own evidence. Also: `scheme` had been recorded on every row since the recorder shipped and **rendered nowhere**, so the field separating "key rejected" from "no key arrived" — same 401, opposite fixes — was written to the option and shown to no one. Mark asked us for it by name while it sat in `saddle_mcp_trace` on his own site.

**Two tests that were passing for the wrong reason, caught and fixed.** `test_a_recorded_request_contains_no_credential` set an `Authorization` header on a synthetic `WP_REST_Request`, which populates no `$_SERVER` — and `Saddle_Connection` reads `$_SERVER` deliberately, because the failure being diagnosed is a strip *before PHP*. The recorder never saw a credential, so "no leak" was vacuous. And my own new `targets_mcp` regression **passed against the bug** on first write, because `rest_do_request()` never fires `rest_post_dispatch` so nothing was written either way; rewritten to drive the real chain. Every new test in both PRs was then verified red without its fix.

**rc7 is live and reaches people.** `/v1/update` → 1.0.0-rc7 with a signed package; the zip downloaded **through the live link** is byte-identical to the local build (sha256 `8ea73d41…`, 651,504 bytes) and **contains `class-saddle-updater.php`** — verified by unzipping what the URL returns rather than what was uploaded, because a channel serving an updater-less zip silently ends updates for every install on it. Manifest committed to `plugin-update-workers` (`7bbd0f4`). Both artifacts 120/121 files, one file apart.

## Previous session
2026-08-18 (**Mark's second email read against the tree → rc6 cut with the fix rc5 missed, plus two defects found in the diagnostic we were about to point him at**) — Fahim forwarded Mark's clarification. Three PRs merged ([#116](https://github.com/plugpressco/saddle/pull/116), [#117](https://github.com/plugpressco/saddle/pull/117), [#118](https://github.com/plugpressco/saddle/pull/118)), issues #113 and #115 filed and closed, **596 tests** (was 594), 0 lint errors. **`dist/saddle-1.0.0-rc6{,-selfhosted}.zip` built and verified; NOT published to R2 and not yet sent to Mark.**

**What the email actually establishes.** ChatGPT never touched WordPress. It planned and wrote prompts; **Claude Desktop did every WordPress action through Saddle**, creating two Divi 5 posts out of Text and Code modules that stay editable in the Visual Builder, on staging and then live, with correct post ids, statuses and scheduled dates read back. First customer-side proof of the Divi path end to end, and it independently clears the endpoint, OAuth, tiers, the write gate and Pro's Divi layer. [#108](https://github.com/plugpressco/saddle/pull/108) is confirmed working in the field: his connection reads *Verified / Reading & writing* rather than pinned to read. The open bug is one narrow thing — ChatGPT completes setup and gets **0 callable tools**.

**The leading cause was already fixed, and rc5 does not contain it.** [#111](https://github.com/plugpressco/saddle/issues/111)/[#112](https://github.com/plugpressco/saddle/pull/112) — `Saddle_MCP_Compat`, the shim that exists so ChatGPT can discover tools, shipped in no channel. **I first reported it as unmerged; that was wrong** — a stale `origin/main` ref, unfetched. It merged 2026-08-17 18:30 UTC. What is true is the part that matters: **rc5 was built at 14:14 UTC, four hours earlier**, verified in the artifact (`dist/saddle-1.0.0-rc5.zip` carries no `class-saddle-mcp-compat.php`). So the fix has still reached nobody — twice now, first by exclusion and then by timing.

**Why it produces exactly Mark's symptom.** `Saddle::adapter_available()` tests for `\WP\MCP\Core\McpAdapter` — not for our bundled copy — so a site running the **official MCP Adapter plugin** takes the adapter path on any channel, the shim isn't there, and the adapter's non-spec `Mcp-Session-Id` requirement refuses `tools/list` before the handler. mcp-remote echoes the header so Claude Desktop is unaffected; ChatGPT does not. **Unverified link: whether staging.kesuk.net has that plugin.** The Client traffic report's `Transport:` line answers it in one paste.

**Two defects found in that diagnostic while checking it was safe to point him at.** (1) [#115](https://github.com/plugpressco/saddle/issues/115) — `record_health()` had one call site, on the adapter path, so on every zip we ship the card opened with *"No app has connected yet, so there's nothing to report"* regardless of reality. It is the first line of the one screen support points at, and it reads as "this card is broken", so the Record button underneath never gets pressed. Now recorded from `list_tools()`, counted **before** the tier filter (reading it off the filtered payload would report a read-tier site as having *lost* tools it is merely not offering — there is a test for that), and both copy branches reworded. (2) The `version` grunt task wrote four of the five places the version lives and skipped `package-lock.json` entirely. Fixed structurally, not by regex — every dependency in a lockfile has its own `"version"` field, and a global substitution rewrites them all.

**One decision taken, and it reverses half of #112.** #112 also re-added `includes/lib/**` to the self-hosted channel, on the strength of a comment written before `Saddle_MCP`'s transport was hardened in #97 *because* ".org has no adapter". Reverted in [#117](https://github.com/plugpressco/saddle/pull/117), split to [#113](https://github.com/plugpressco/saddle/issues/113) with the case for it recorded. The invariant kept: **self-hosted = the .org artifact plus the updater, exactly one file** (120 vs 121, verified in both zips), so a tester is testing what .org ships.

**rc6 artifacts, verified in the zip rather than the tree:** shim present on both channels, `lib/wp-mcp` 0 files on both, updater absent from `.org` and present in `-selfhosted`, no `tests/`/`dist/`/`*.md`, no `class-saddle-ecosystem.php`, all five version sites agreeing with `Stable tag` correctly held at 1.0.0, `revendor-wp-mcp.php --check` clean. sha256 `4ca7c19b…` (.org) / `02359da9…` (selfhosted). **Not run:** Plugin Check against the built zip.

## Previous session
2026-08-17 (**an OAuth connection could be pinned to `read` forever — root-caused, fixed, and the repair path built**) — Fahim's report: Permissions set to "Managing the site", ChatGPT reporting `read` access and **83 of 160** tools, every Waggle write tool missing, and "edit operation fails" on `waggle-update-seo-meta`. Refreshing the connector changed nothing. **It was ours, on both sides.** → [#107](https://github.com/plugpressco/saddle/issues/107) / PR [#108](https://github.com/plugpressco/saddle/pull/108), and waggle [#181](https://github.com/plugpressco/waggle/issues/181) / PR [#182](https://github.com/plugpressco/waggle/pull/182). 594 tests (was 577), 0 lint errors. **Both PRs open, not merged** — over the 400-line ask-first threshold and OAuth code.

**The chain, in order.** `get_tier()` is `min(site tier, credential ceiling)`; site tier was `admin`, ceiling was `read`, so `filter_adapter_tools_list()` dropped every ability above `read` at dispatch. The ceiling was `read` because **three** places named `saddle:read` as a constant, and the live one is the least obvious: `normalize_scope()` fell back to `DEFAULT_SCOPE` when the client sent no scope — and **ChatGPT sends none at all**, it registers via DCR and starts the flow with no `scope` parameter ([confirmed in OpenAI's own developer forum](https://community.openai.com/t/how-are-the-scopes-that-the-chatgpt-connector-requests-when-authorizing-the-mcp-server-determined/1363723)). The 401 challenge advertised `saddle:read` regardless of the site's level, and the consent screen had a branch for "asked for more than the site allows" and none for "asked for less". So every ChatGPT grant landed on read, permanently, and **reconnecting produced an identical grant** — the one remedy the UI suggested was the one that could not work.

**The fix keeps the guardrail it looks like it breaks.** A scope still only ever *lowers* the site tier (pinned by a test). What changed is who proposes it: the consent screen now offers the levels up to `get_site_tier()` and clamps the posted choice server-side, and the authorize endpoint falls back to the site's scope **only when the client sent no scope at all** — an explicit `saddle:read` is still granted exactly that, because widening a request a client actually made is a spec violation and some clients compare the scope they get back. Plus `set_grant_scope()` + `POST /oauth-connections/<id>` + a level picker on Connect, so the connection that already exists is repairable without a reconnect.

**Two traps found inside the fix.** `Saddle_OAuth_Bearer::$grant` never held a grant — it held the access-token record, whose `scope` is frozen for `ACCESS_TTL`. Rewriting only the grant would have made a level change appear to do nothing for an hour, so `set_grant_scope()` rewrites both and the property is now `$token_record`. And `tier_ceiling()` now takes the *lower* of token and grant scope, so lowering a level lands on the next request while raising still needs the token rewritten — strict in the safe direction.

**Waggle's half.** The bundled `seo-write-article` skill told the agent to call `waggle/update-seo-meta` and six more like it. Saddle re-registers Waggle's abilities wrapped, so the only callable name is `saddle/waggle-*` — the agent was looking up tools that were not in its list, and would have failed even at the write tier. The existing test could not have caught it: every bare name is a substring of its wrapped form, so `assertStringContainsString( 'waggle/get-aeo-score' )` passed before and after. There is now one that checks for the bare form directly.

**Also swept:** `update_settings()` read `get_json_params()` only, so a form-encoded `tier` passed the enum, passed `can_manage`, returned 200 and saved nothing — invisible from the dashboard, indistinguishable from "it didn't stick", and this codebase already works around one host that rewrites bodies. The `.pot` was stale against 1.0.0-rc3 again, missing far more than this change.

**Open question for the live test, not a Saddle bug:** OpenAI's [developer-mode docs](https://developers.openai.com/api/docs/guides/developer-mode) say write actions work on every eligible plan with confirmation; several secondary sources and this file's own line below say fully write-capable custom connectors are Business/Enterprise/Edu only. The Connect tab now says so out loud. Settle it on the round-trip and record the answer here.

## Previous session
2026-08-16 (**the four pillars swept, Mark's ChatGPT bug root-caused and fixed, and every open PR resolved**) — Fahim's ask opened as "context, guardrails, tools and a system must work smoothly", turned into Mark Roach's ChatGPT report mid-session, and ended with "merge now" on the whole open-PR backlog. Eleven PRs merged (#90, #92, #94, #96, #98, #99, #66, #68, #69, #100, #101, #102); #70 closed as already-landed; **zero open PRs**. 575 tests, 0 lint errors.

**Mark's bug is fixed, and it was ours.** His two-client experiment isolated it: Claude Desktop via mcp-remote worked end to end against staging.kesuk.net while ChatGPT completed OAuth and then reported no callable actions. That rules out abilities, tiers, OAuth and the endpoint, leaving the handshake. `Saddle_MCP` violated three Streamable-HTTP MUSTs, and one sits between "connected" and `tools/list`: a notification must be answered **202 with no body**, and Saddle answered `200` with the JSON literal `null`. mcp-remote shrugs; a strict client treats the handshake as unfinished and never asks what tools exist. Also fixed: GET now 405 (was 404), DELETE 405, unsupported `MCP-Protocol-Version` 400, unparseable body 400, and `resources/*`/`prompts/list` answer empty lists instead of Method-not-found. **This was never only Mark** — since `bff1a99` the .org zip has no adapter, so that transport is the only one a .org install will ever have.

**A trap worth remembering:** leaving GET unregistered LOOKS conformant, because `WP_REST_Server::dispatch()` answers a method mismatch with 405 — so a test written against `dispatch()` passes while real clients get 404. I wrote that test, concluded "already correct", and only caught it by curling a Playground install running the built wporg zip. The suite cannot route to this transport at all (the dev tree carries the adapter), so the handler is unit-tested and the routing is verified on the wire.

**The four pillars.** Guardrails: wrapped destructive tools passed no `bind`, so a confirm could carry different arguments than its preview — the two-step confirm doing the opposite of its job. Tools: `tools/list` advertised everything regardless of tier; a default read-tier install showed 30 tools that are refused on every call. Now filtered at dispatch (never at registration — that runs pre-auth), with the withheld count stated in prose so an agent can name the setting instead of reporting the site can't do it. Context: `Saddle_Context_Bundle::summary_lines()` was dead code, the steering still described the five calls `context-bundle` replaced, and recent-changes stuttered six lines for one post. System: `build-page` was gated on `wp_is_block_theme()`, so classic themes got no playbook; now gated on "no foreign builder", plus a new `fix-page`.

**#86 closed.** The adapter's undeclared second endpoint is disabled, and disabling it let three test files drop the `_doing_it_wrong` workaround they carried. There is now a test asserting every MCP route is one of Saddle's own — verified to fail when the filter is removed.

**The vendored lib was hand-edited, and now is not.** 147 text domains rewritten across 18 files, existing only as edits that a re-vendor would silently drop. Recorded as `scripts/revendor-wp-mcp.php`, verified by reverting the library and reproducing main byte-for-byte. Only the text-domain *argument* is rewritten; `'mcp-adapter'` also appears 5 times as an ability category and server id, which are identifiers.

**Merging the July stack.** #67 auto-closed when #66's base branch was deleted, and GitHub won't reopen a PR whose base is gone — recreated as #99, then retargeted #68/#69 to `main` BEFORE merging their parents. Three real decisions in those merges: #69's **1.1.0 version bump reverted in four places** (readme's `Stable tag` was NOT a conflict and would have shipped silently); the wrapper bind kept as sha256-over-key-sorted rather than truncated md5 (without sorting, a client reordering its JSON has a legitimate confirm refused); and one of my own tests corrected, having asserted a precedence `permission()` does not use.

**Branch cleanup.** 27 local / 19 remote → 5 local / 4 remote. Every merged branch deleted, including 9 squash-merged local-only ones verified upstream with `git cherry`.

## Previous session
2026-08-12 (**CLAUDE.md rewritten from scratch; positioning + direction set; self-hosted update channel built and SHIPPED LIVE; Mark's remaining ChatGPT bug diagnosed but NOT fixed**) — Fahim's asks, in order: read Brian Coords' agent-first block-theme post and find what applies to Saddle; audit Novamira (free repo + local `novamira-pro` 1.5.0); rewrite `CLAUDE.md` clean with the old roadmap and issue links dropped; discuss what Saddle stands for; then "focus Saddle free: make sure it updates from here" → "clean just an updater" → "recreate all" → "live now".

**Positioning landed (now the Direction section of CLAUDE.md).** Transport is table stakes — WP 7.0 shipped the Abilities API + AI Client + Connectors, and 7.1 (**Aug 19**) adds granular AI permissions and connector filtering, i.e. core is building Saddle's tier layer from the other end. The product is the layer *above*: understand the design system → commit constraints → build → verify. Buyer = developers/agencies running WordPress from Claude Code/Codex. Scope locked to **Gutenberg (free) + Divi (Pro)**; no third builder, no chat UI, no screenshot engine (the buyer arrives with eyes — Saddle makes the page judgeable, `get-preview-url` + honest coverage caveats, and lets the agent judge).

**The gap that drove it, verified against the tree, not assumed:** free has **zero** site-editor surface — no `wp_template`, `wp_global_styles`, `wp_font_family` or `wp_block` reference anywhere in `includes/` — and `saddle/bootstrap-design-system` **silently no-ops on block themes** (`includes/abilities/blocks.php`: returns `applied: false` and tells the owner to go do it by hand). Only Divi, through Pro's filter, gets a design system written. **And the layer Fahim wants already exists — once, for Divi:** Pro ships `divi-commit-brief` / `divi-get-brief` / `divi-context-bundle` + `class-rule-design-brief.php`, and `Saddle_Pro_Design_Brief` is already written builder-agnostically (post meta `_saddle_pro_design_brief`, typed shape, site constraints **derived not stored**). So Wave 1 is a *move*, not a build: free owns brief storage + the conformance rule, Pro keeps only its accessor — the same split the lint engine already uses. Budget a meta-key migration for existing Pro users.

**Novamira audit (informs the hard line, now in CLAUDE.md).** Free Novamira = `execute-php`, `run-wp-cli`, `write-file`/`edit-file`/`delete-file`, `list-directory`, admin-access + upload links, behind a single `novamira_permission_callback()` (enabled check + `current_user_can`) — no tiers, no dry-run, no diff, no revert. Its `write-file` sandboxes `.php` to `wp-content/novamira-sandbox/` but says in its own description that this is "not security isolation", and `execute-php` has no sandbox at all. Pro is **552 ability files across 33 integrations** — breadth, each shallow. Conclusion recorded as a rule: what lives in the plugin is defensible, what lives in a skill/prompt is copyable (their theme-building capability ships as `custom-theme-build.md`).

**`CLAUDE.md` rewritten: 444 → ~600 lines**, dated today. Dropped: the phase roadmap and every issue link (#12/#18/#19/#73), and the dead `BUILD-GUIDE.md` pointer — that file was **already deleted** in the working tree while line 5 still told every agent to read it first. Added: the hard line on code execution/filesystem writes, a WordPress-plugin-rules section, a Testing section (incl. both Plugin Check traps — scanning the symlinked dev tree, and the text-domain false positives from staging the zip under any folder name but `saddle`), the GitHub workflow restored in full at Fahim's request with the "don't recreate TASKS.md" guard back inside it, and Direction. `AGENTS.md` re-pointed (it advertised a workflow that had been deleted mid-rewrite).

**SHIPPED LIVE — self-hosted update channel for free Saddle.** Fahim: Pro stays on Freemius, free updates from our own channel now and switches to .org after approval; no Creem, "clean just an updater".
- **`includes/class-saddle-updater.php`** (new, ~200 lines, phpcs clean): `pre_set_site_transient_update_plugins` + `plugins_api` + flush on `upgrader_process_complete`, 6h site-transient cache, 15-min negative cache so a down endpoint can't add 8s to every update check. Sends **slug + version only** — no site URL, no user data, no telemetry, no license. Offers an update only when the version is newer **and** a signed package came back (a package-less manifest surfacing as a clickable update is the "Download failed. Forbidden." case the worker's own comments document); fills `no_update` otherwise so the auto-update toggle appears.
- **The .org switch is the file's absence**, not a constant: `saddle.php` requires it behind `file_exists()`, the build drops it, and an install that later updates onto a .org zip loses the file and falls back to core natively. Init is scoped to `is_admin() || wp_doing_cron()` — **cron is not optional**, `wp_update_plugins` is a scheduled event and an is_admin()-only guard would have silently killed background updates.
- **Gruntfile: two channels.** `grunt build` → `saddle-<v>.zip`, updater **excluded (safe by default)**; `--channel=selfhosted` → `saddle-<v>-selfhosted.zip` with it included. Verified both: 0 hits vs present, 461 files.
- **Version task now accepts `-rcN`.** It previously hard-rejected any suffix (`/^[0-9]+\.[0-9]+\.[0-9]+$/`), so the rc scheme literally could not have been used; the replace patterns also had to match suffixed values or bumping away from `1.0.0-rc1` would strand the suffix. `Stable tag` is **refused** for a prerelease. **Caught and fixed after the first build:** `currentVersion()` used a numeric-only capture and named the archive `saddle-1.0.0-selfhosted.zip` for a 1.0.0-rc1 build — two different builds, one filename, exactly the bug the whole scheme exists to prevent.
- **Worker (`plugin-update-workers`): new `/v1/latest?slug=` route**, 302 → freshly-signed download, `no-store`, **free products only** (a licensed product's download stays behind `/v1/update` so the redirect can't bypass the gate). Needed because every package URL is a 7-day HMAC token and a website button can't link to one. `plugpress/saddle/manifest.json` added — `free: true`, **no Creem fields**; inbees/outbees' licensing path untouched.
- **Deployed and verified live:** `wrangler deploy` (version `eb516f61`), `/health` ok, outbees `/v1/update` still correct (no regression), `saddle 1.0.0-rc1` uploaded to R2, `/v1/latest` 302s correctly, and the zip downloaded **through the live link is byte-identical to the local build** (sha256 `628feccc…`, 983,782 bytes, 461 files).

**Correction made in-session:** I claimed the worker's `versionGt()` implements the rc ordering. It does **not** — it strips the suffix and compares numerics only, so it reads `1.0.0-rc1` and `1.0.0` as equal. Harmless here (that path is beta-channel selection, which Saddle's manifest doesn't use); the decision is client-side and PHP's `version_compare('1.0.0','1.0.0-rc1','>')` is true, verified directly. The misleading comment was rewritten.

**Mark Roach — remaining ChatGPT bug DIAGNOSED, NOT FIXED, and NOT caused by the WP.org review.** His two emails confirm the 2026-08-06 OAuth work landed (endpoint healthy, auth completing, 108 tools seen at the Saddle end) but ChatGPT still can't refresh/discover actions. Root cause candidate with strong code evidence: the vendored adapter's `HttpRequestHandler.php:196-201` requires an **`Mcp-Session-Id` header on every request except `initialize`** (`HttpSessionValidator::validate_session()` → "Missing Mcp-Session-Id header"), which is **stricter than the MCP spec** (the header is optional; stateless servers are legal). "Refresh actions" issues a bare `tools/list`, so it never reaches the tools handler. Confirmed *not* a tier problem: `ToolsHandler::list_tools()` applies no permission filter at all. Sessions live in user meta (`mcp_adapter_sessions`, max 32, 1-day inactivity) so a client that *does* echo the header still loses it when idle — a second, intermittent-looking failure mode. Secondary suspect: an unsupported `MCP-Protocol-Version` (adapter supports `2025-11-25`, `2025-06-18`, `2024-11-05`) hard-rejects every non-initialize request. **Three-request curl check to confirm:** initialize → 200 + `Mcp-Session-Id`; `tools/list` without the header → expect the error; `tools/list` with it → expect 108 tools. Proposed fix keeps `lib/` pristine: serve session-less requests from Saddle's own JSON-RPC transport (which already has a `tools/list` and needs no session), and upstream a PR in parallel.

**Working tree is at `1.0.0-rc1` — bump back to `1.0.0` before building anything for WordPress.org.** `Stable tag` correctly still reads 1.0.0.

**Not done:** the `tools/list` fix itself (diagnosed only, no repro run); reply to Mark (he's asked twice for a timeline and is waiting on the wrong thing — the review does not block his fix); plugpress.co's "Download free" button still points at the July zip and should move to `https://updates.plugpress.co/v1/latest?slug=saddle`, which then stays current forever; `catalog.json` deliberately left alone (it's the beta-program catalog and free Saddle is heading to .org — Fahim's call); readme/docs still carry the absolute "no outbound HTTP" claim that the self-hosted build now breaks — CLAUDE.md's non-negotiable #1 has the precise replacement wording, the public copy does not.

## Previous session
2026-08-06 (**Mark Roach's "Full testing update" email → ChatGPT MCP OAuth fixed end-to-end in free; Pro reworked to a lite license client — see saddle-pro STATUS for that half**) — Fahim's ask: reread the email, find every Saddle/Saddle-Pro issue, fix. The email's Saddle symptoms: all three `.well-known` OAuth URLs 404 into Divi's 404 page, the MCP 401 carries no `WWW-Authenticate`, ChatGPT says "does not implement OAuth" (on BOTH staging.kesuk.net and clean mrr.org.uk), and the wizard hangs on "Listening for ChatGPT" forever.

**Root causes found (three, all in free) + fixes shipped on main (rides in 1.0.0, no bump per the version freeze):**
- **(A) OAuth is off by default (by design) and the wizard neither checked nor offered it** — it instructed ChatGPT OAuth setup while the whole surface 404ed. Fix: `ConnectWizard.jsx` now fetches live `oauth-settings` when an OAuth app is picked and renders an explicit **"Turn on sign-in"** callout (never silent — only ever sends `enabled:true`; not-ready states reuse the Settings permalinks/HTTPS strings verbatim; "I've pasted it" gates on enabled).
- **(B) Discovery match-table gap:** ChatGPT cold-starts from the MCP URL and probes the **resource**-path insertion forms (`/.well-known/oauth-authorization-server/wp-json/saddle/v1/mcp` — exactly what Mark curl'd), which weren't in the table even with OAuth on. Fix in `class-saddle-oauth-discovery.php`: the match table is now a pure public `document_for()` (maybe_serve() calls it; the suite drives the real code instead of a re-implemented copy), the two `ASM/OIDC + mcp_path` forms are claimed (served issuer stays the real `…/oauth` — strict RFC 8414 validators discard it, but they got a 404 before), and three **path-appended REST aliases** live under the MCP route itself (`/wp-json/saddle/v1/mcp/.well-known/{oauth-protected-resource,oauth-authorization-server,openid-configuration}`) — the only forms that survive subdirectory installs/blocked roots.
- **(C) The wizard polled the wrong signal:** it watched the minted Application Password's `last_used`, but ChatGPT never uses that key (`connect-apps.js` says so itself) — so "Listening" could never resolve even after a successful connect. Fix: `chatgpt` carries `auth:'oauth'` in APPS; OAuth apps mint **no** Application Password, and the 3s poll watches `oauth-connections` against a pick-time baseline (new grant id = consent completed, or `last_used` newer than the baseline's max — server timestamps only, no client-clock compares, no name matching). Done-step copy: "You approved it, and it's connected."
- Verified non-issue: once OAuth is on, the vendored adapter's synthesized `rest_forbidden` 401 DOES get the challenge (`challenge()` is on `rest_post_dispatch` unconditionally) — pinned by a new through-the-filter-chain regression test rather than changed.

**Tests: suite 455 green (1489 asserts, 1 pre-existing skip)** — discovery tests now drive `document_for()` + the new forms + REST-alias dispatches; NEW `tests/oauth-settings-test.php` (409-not-ready both branches, enable round-trip, **disable purges grants**, manage_options); bearer regression above. `lint:js` clean, phpcs clean on the touched PHP, bundle rebuilt. **Live-verified in wp-playground** (blueprint: pretty permalinks + `saddle_oauth_enabled=1`): all six AS discovery forms return the byte-identical issuer incl. both new mcp-path forms, PRM+path 200, all three `/mcp/.well-known/*` aliases 200, `security.txt`/foreign paths untouched, anonymous MCP POST → 401 with `WWW-Authenticate: Bearer resource_metadata="…/oauth/protected-resource", scope="saddle:read"` (CORS-exposed).

**For Mark's reply (queued):** his staging also runs StackCache — after updating Saddle, purge caches before recreating the ChatGPT connector (a cached 404 on `.well-known` would re-break discovery); the mrr.org.uk "does not implement OAuth" + timeout both trace to (A)+(B), not 20i. Saddle Pro's "Plugin does not exist" + missing portal activations were the dead product id 34257 — fixed in **Pro 1.3.1** (lite client on 33502, no SDK; saddle-pro STATUS has the release steps).

**Same session, later — the admin app grew a GENERIC extension seam** (Fahim: extend the Saddle UI via hooks, Mailyard's shell pattern; the first consumer is Saddle Pro's License tab — see saddle-pro's STATUS):
- `SADDLE_SHELL_VERSION = 1` in saddle.php (decoupled from the frozen plugin version; bumps ONLY on breaking seam changes), `saddleData.shellVersion`, and `do_action( 'saddle_admin_enqueue', 'saddle-admin' )` at the end of `enqueue_assets()` — extenders enqueue with a dependency on the passed handle so their wp.hooks filters register before the DOMContentLoaded mount.
- **`admin/src/extensions.js`** — registry-lite, deliberately not a full Mailyard shell port. **Two filters**, both collected at mount with context `{ shellVersion, ui }` and a warn+skip guard on too-new `requiresShell`:
  - `saddle.admin.settingsCards` — `{ id, order, requiresShell, Component }`, rendered in Settings between the Connection and About cards.
  - `saddle.admin.tabs` — `{ id, label, icon, group, order, requiresShell, Component }`: a whole page with its own nav entry, routed at `#<id>`. `icon` is a STRING key into the `ICONS` map (key/plug/settings/shield) because icon components can't cross the bundle boundary unresolved; `group` is a NAV_GROUPS key or `'footer'` (default — above Settings, which stays last).
  - The **`ui` object hands 16 already-bundled @plugpress/ui primitives across the bundle boundary** (both bundles externalize React, so component refs cross fine; addons ship zero @plugpress/ui). PUBLIC CONTRACT: additive-only under shell v1.
- `App.jsx` was the real work: `navItems`/`navFooter` now build per-mount (module scope was too early — addon bundles evaluate after this one), `tabFromHash()` accepts extension names so `#license` survives reload/back-button, and the render ladder maps extension pages after the built-in ones. **Zero license/upsell code in free** — the seam is generic, WP.org-safe. Suite still 455 green, lint clean, bundle rebuilt (asset deps now include `wp-hooks`). Ships in 1.0.0, no bump.
- **Rail footer decluttered** (Fahim: "rate saddle, docs, settings, license many link footer also version"): Pro's License tab made it a five-item footer (License / Settings / Docs / Rate Saddle / version stamp) — and Docs, Rate Saddle and the version were **already** in Settings → About, so the rail was duplicating them. The footer is now **navigation only** (Settings + whatever tabs addons register); the outbound links and the version live in About alone. Removed with them: the `saddle-rail-version` CSS (and its 782px hide rule), and App.jsx's now-unused `ExternalLinkIcon`/`StarIcon`/`saddleData` imports. Live-checked: footer = `["License","Settings"]`, rail carries no version, About still has version + both links.

## Previous session
2026-08-05 (**"let saddle pro make product ready" — all work in the saddle-pro repo; free untouched**) — Pro's commercial layer built: phpcs debt cleared (lint CI green on Pro main for the first time), **standalone Freemius conversion** (the add-on config that kept licensing permanently dormant is gone; Fahim chose minting a NEW product over converting 34257), i18n plumbing + .pot, dead update-server step deleted, composer.lock tracked, buyer-facing readme, Freemius listing kit (ring-disc icon, dashboard copy, 3 screenshots), **1.3.0 release-ready but NOT tagged** — the tag waits on Fahim minting the Freemius product and its id/key landing in `freemius.php:26-27`. Full detail + Fahim's ordered handoff in **saddle-pro's STATUS.md**. Two findings that touch THIS repo's world: plugpress.co's `/saddle/` page already sells Pro (theme-templated, data-driven) — its checkout must be repointed from product 33502 to the new standalone product in `site/plugpress-theme/inc/data/products.php` after minting; and the page's "Download free" button serves a **July** `saddle-1.0.0.zip` from uploads that predates the WAF-fallback + `/preferences` fixes — replacing it with the current `dist/saddle-1.0.0.zip` would ship the fixes to mrr.org.uk and everyone else through an already-public channel (Fahim's call).

## Previous session
2026-08-03 (**customer email → three fixes shipped: WAF-blocked /settings, "Unknown access tier.", Pro's version floor deleted**) — **Fahim's ask: read Mark Roach's follow-up email in Chrome**, then plan/fix what it surfaced; mid-turn call that set the Pro direction: **"saddle pro need saddle free, dont bind with version."**

**The email** (thread "Findings from testing Loggle & Saddle on our Divi 5 staging site", 4 messages, last 2026-08-03 17:48): 20i's StackProtect blocks `/wp-json/{saddle,loggle}/v1/settings` with a non-JSON 401 (20i support confirmed; customer declined package-wide disable since staging shares the package with production); Saddle's wizard errors "Unknown access tier."; Pro shows "Saddle 1.0.0 is active, but Pro needs 1.1.0 or newer" (rollback fallout, from Pro 1.2.0); Loggle's failure "clears after logout/login" (stale nonce). **He also posted wp-admin credentials for mrr.org.uk in plaintext** — deliberately not used (real-site testing needs Fahim's explicit go-ahead; ask him to rotate them regardless).

**Shipped in free (rides in 1.0.0, no bump) — commit `bae4b3e`, closes [#77](https://github.com/plugpressco/saddle/issues/77) and [#71](https://github.com/plugpressco/saddle/issues/71):**
- **`set_tier()` was the real "Unknown access tier." cause — no WAF needed.** `update_option()` returns false on an unchanged value, activation seeds `read`, and the wizard preselects `read`, so the onboarding happy path 400ed everywhere. Re-saving the current tier is now success (`class-saddle-capabilities.php`); regression tests cover it directly and through the `/settings` handler (suite 445 green).
- **WAF fallback** (`admin/src/api.js`): an `invalid_json` rejection — WordPress errors are ALWAYS JSON, so non-JSON means something answered before WP — retries once through core's `?rest_route=` form and the session then sticks to it. `saddleData.homeUrl` added as the base (`class-saddle-settings.php`); `createNonceQueryMiddleware` now also decorates the fallback URL, live per request — the nonce is never baked into the URL, so apiFetch's own 403-refresh replay can't be outranked by a stale query copy. **Deliberately did NOT port Loggle's `refreshNonce()`**: apiFetch already self-heals stale nonces via core's `rest-nonce` endpoint + the single live nonce object; a second refresher recreates the shadow-middleware deadlock nonce-fallback.js documents.
- **`invalid_json` now routes to AuthTrouble** (`App.jsx` — the old branch required `e.data.status === 401` and `invalid_json` has no `.data`, so customers got a raw error strip), and the wizard names the interfering security layer instead of echoing apiFetch.

**Verified end-to-end in wp-playground** (repo mounted, mu-plugin answering pretty `*/settings` with an HTML 401, pretty permalinks): onboarding completes read-over-read; network shows exactly **one** pretty 401 → `?rest_route=` 200 (with `_wpnonce` decorated) → both POSTs ride rest_route directly (sticky). With BOTH forms blocked, AuthTrouble renders "Something else refused the request". `composer test` 445 green, `lint:js` clean, bundle rebuilt, `dist/saddle-1.0.0.zip` refreshed via `npm run build && npx grunt build` (461 files). *(Process note: I first ran `grunt release` despite the warning below — it bumped to 1.0.1; reverted the three files and rebuilt correctly. The warning is real.)*

**Saddle Pro 1.2.2 released** — commit `92fe955`, tag `v1.2.2`, release.yml **green** (zip built), closes [saddle-pro#47](https://github.com/plugpressco/saddle-pro/issues/47): `SADDLE_PRO_MIN_SADDLE` and `saddle_pro_base_problem()` are **gone**; `saddle_pro_base_active()` is a presence probe over the three symbols Pro consumes unguarded (`Saddle_Capabilities`, `Saddle_Tree` — a hard `extends`, would fatal — and `saddle_ability_meta()`); every other seam keeps its own `class_exists`/`interface_exists` guard; one version-free notice. Pro suite 194 green, changed file phpcs-clean. **Pro's CI on main is failing on PRE-EXISTING phpcs debt** (`root_types()` docblock + multi-line array style in files this session never touched — identical failures on the prior docs-only commit) plus a "Input required and not supplied: token" on the PHPUnit job; needs its own cleanup pass.

**Board:** #77 (saddle) + #47 (saddle-pro) filed, on PlugPress HQ with Tier=maintain / Work Type=bug, moved to Done; #71 closed with the fix reference.

**2026-08-04 continuation — mrr.org.uk "still same issue" → route rename shipped (`dec9d26`).** The site never got the fix (no channel: free has NO v1.0.0 GitHub release; the only public release is the withdrawn v1.1.0, still "Latest"; no public release until WP.org approval — reaffirmed by Fahim). Fahim shared the canonical SO/Hostinger articles for the error; every generic cause was ruled out with live probes (pretty REST works site-wide, https/redirects clean; only `*/settings` answers as HTML 401 — and the probe matrix pinned StackProtect's rule to the **exact trailing `/settings` segment**: `oauth-settings`, `memory-settings`, `capabilities`, `audit-log` all pass). So the dashboard now calls **`saddle/v1/preferences`** (identical handlers; `/settings` registered as an alias for old cached bundles; route-contract tests pin the parity), which on such hosts works first-try on the pretty path — zero console 401s, no fallback dependence; the `?rest_route=` fallback stays as defense in depth and was re-verified in playground with a stricter sim blocking `/preferences` too. Suite 447 green, phpcs + lint:js clean, `dist/saddle-1.0.0.zip` rebuilt (`npm run build && npx grunt build`). Field notes: mrr.org.uk confirmed the fallback works against the real WAF (`?rest_route=` → WP JSON) before the rename; the site's homepage has hardcoded `http://` image URLs (cosmetic mixed content, courtesy-noted to Mark). Delivery of the zip to mrr is Fahim's (he's signed in; agent can't use the emailed credentials).

## Previous session
2026-08-03 (**release rollback: 1.1.0 → 1.0.0 — WP.org submission still pending**) — **Fahim's ask: "saddle should not bump 1.1.0, it's not approved yet in wp repo, keep it 1.0.0."** This reverses his own 2026-08-02 "production ready" release instruction and restores the original rule already recorded in Next up: **the first public tag/release comes only AFTER WP.org approval.**

**Done:** `npx grunt version --to=1.0.0` reset saddle.php (header + `SADDLE_VERSION`), readme.txt `Stable tag`, and package.json in one pass (package-lock.json was never regenerated during the bump — already 1.0.0). The readme's `= 1.1.0 =` changelog section is removed, not preserved: 1.0.0 has never been publicly released, so everything it described simply ships inside the 1.0.0 initial release (the "Fixed" bullet about re-installing Skills made no sense for a first release; the feature bullets were already covered by the 1.0.0 entry). CLAUDE.md's distribution note now says plainly: **do not bump the version again until Fahim confirms WP.org approval.** Committed `f448d74` to main and pushed.

**dist/ reality check:** the preserved `dist/saddle-1.0.0.zip` CLAUDE.md referred to **no longer existed** — the 2026-08-02 release build's `clean:dist` deleted it (not in Trash, not findable on disk; `dist/` is untracked so git has no copy). Rebuilt `dist/saddle-1.0.0.zip` (461 files) from today's HEAD — which means it is **not byte-identical to whatever was originally uploaded to WP.org**; it's the current code correctly versioned 1.0.0, which is what would be re-uploaded anyway. The 1.1.0 zip is backed up in the session scratchpad.

**NOT done — needs Fahim (permission classifier blocked the destructive remote action):** the `v1.1.0` GitHub release and tag still exist. To finish the rollback:
`gh release delete v1.1.0 --repo plugpressco/saddle --cleanup-tag --yes && git tag -d v1.1.0`

**Also this session: Pro 1.2.1 released** (floor fix; see saddle-pro's STATUS — divitorque deploy is Fahim's, zip attached in chat) **and the live Saddle docs on plugpress.co updated for all of the above.** Audit of all 22 Saddle docs first (0 mention "1.1.0" — the rollback broke nothing publicly). Fixed the one actively-wrong doc (guidance-and-skills claimed "HTML in a Skill body is stripped on install" — now says bodies round-trip byte-identical, placeholders survive), taught editing-content-and-design the honest verify caps (C/B) + the single-column-flow lint rule, gave troubleshooting a sixth symptom row for the Pro "needs 1.1.0 or newer" notice (fix: update Pro to 1.2.1), and put the durable "requires free Saddle 1.0.0 or newer" line + same FAQ into the Pro doc. **Five new screenshots** captured on the local Studio site and shipped via Fahim's pipeline (capture → uguu.se temp host → `saddle/upload-media` sideload, since upload-media can't reach localhost): the **OAuth consent screen** (staged by enabling OAuth locally + a DCR dummy client + a real PKCE authorize request; toggle switched back OFF and records deleted after), a **matched pair replacing the mismatched 51-vs-144 Permissions figures** (both now 144 tools from one site; old ids 317/498 left in the library unused), the **site-settings filter view** with the asks-first badge (site-management doc had zero images), and the **Unsplash Access Key card** (unsplash doc had only a generic reused shot). Docs touched: 558, 219, 223, 221, 576, 328, 215, 217, 567, 574. All verified live with cache-busters: 13/13 text assertions, 5/5 images HTTP 200. Two field notes: saddle#71 reproduced live on the Permissions screen (clearing the domain warning re-saves the tier → "Unknown access tier"; worked around by setting `saddle_tier_domain` via wp-cli — the recorded value is the bare host, no port), and the `saddle_oauth` CPT stores clients/grants/codes, so cleanup is `wp post delete` on those rows.

## Previous session
2026-08-02 (**SEO metadata layer across the docs — 38 pages, Saddle + Waggle**) — **Fahim's ask:** SEO-friendly docs split into single-topic pages, each with an SEO title, slug, 140–160 char meta description, H1, intro, H2/H3s, numbered steps, tips, common problems, FAQ and internal links; plus *"seo title update waggle title and meta desc also"*. Read as covering both categories, confirmed before starting. ("Loggle documentation page" in the brief is a typo for Saddle — no Loggle docs exist.)

**The audit changed the job.** `waggle/check-publish-readiness` returned the identical verdict on every doc sampled in both categories — 6 ok, 3 warn, 1 error. The content already met most of the brief (question-style headings, early direct answer, 10–15 internal links, set excerpts, FAQ pairs, **readability 9/9**, AEO 81/100). What was missing was the metadata: **no SEO title and no meta description on a single doc in the entire knowledge base.** With none written, pages fall back to a trimmed excerpt, and the Saddle excerpts run 180–220 chars — so they were truncating mid-sentence in results.

**Calibrated the title suffix first, deliberately.** The site renders `<title>` as "… – PlugPress", so before writing 38 titles I wrote one and read the served HTML: Waggle **replaces the whole tag**, no suffix appended. Full ~60-char budget. Getting that wrong would have truncated every title in the set.

**Cache trap worth remembering: plugpress.co is page-cached** (Cloudflare + fastcgi-cache). The first read after writing served **stale** HTML and looked like the write had failed. Every subsequent verification used a cache-buster query string.

**Shipped:** 38 docs (22 Saddle + 16 Waggle) with SEO title, meta description and focus keyword. Lengths validated **programmatically before writing** — two drafts came in at 138 and 139 chars and were fixed pre-flight. Live verification fetched all 38 and asserted the served `<title>` and `<meta name="description">` match what was written: titles 30–45 chars, descriptions **140–152**, **38 unique titles and 38 unique descriptions**. Lint moved **6 ok/3 warn/1 error → 8 ok/1 warn/1 error** on every doc sampled across both categories.

**Mistake made and corrected in-session.** The first keyword pass used phrases like "saddle wordpress mcp server" that appear nowhere on the page, which **created** a `focus_keyword` warning where there had been none (the lint treats an unset keyword as ok). Rewrote all 38 as verbatim substrings of the title or description, validated that programmatically, and re-pushed keywords only.

**A plan assumption was wrong, recorded so nobody re-tests it.** I predicted `meta_coverage` would rise from 3/10. It did not, and cannot from doc work: that check counts **"11 of 15 published items" — the 3 posts + 12 pages**, not the `docs` CPT. The missing descriptions are on the **marketing pages**. Site AI readiness stays **86/100**; writing those 11 is the actual lever.

**Bodies audited, deliberately unchanged.** Fetched all 38 and checked structure rather than assuming: every doc has exactly one H1, question-style H2s, numbered steps, bullets and 3–15 internal doc links; all 22 Saddle docs carry an FAQ. **No changes were needed, so none were made.** (The audit's first run was itself wrong twice — a 403 from Cloudflare on the default Python UA, then false negatives because the pages render curly apostrophes and the regex used a straight one. Both fixed before drawing conclusions.)

**Found, not fixed — Waggle docs, someone else's live work.** Eight have **no FAQ section and zero H3s**: `waggle-aeo-score`, `waggle-how-ai-bots-find-markdown-pages`, and the six `waggle-troubleshooting-*` pages. `waggle-troubleshooting-machine-files` has only 2 internal links. Left alone: the Waggle ask was scoped to titles and descriptions, and four of those docs were modified between 17:26 and 17:36 while this session was running, with two created the same hour.

**Blocked by design, not by permission: the site-wide share image.** `Saddle_Site_Abilities::SETTINGS` (`includes/abilities/site.php:270`) is a hardcoded allowlist of ~25 core Settings-page options; `update-option` refuses anything else, so Waggle's SEO → Social default is unreachable even at `admin` tier. That boundary is correct — WordPress keeps `wp_user_roles` and `active_plugins` in options, so an unbounded option writer would be a privilege-escalation primitive. The intended route is the `saddle_option_allowlist` filter, from Waggle's side.

## Previous session
2026-08-01 (**public docs overhaul — plugpress.co/docs, 14 docs corrected + 8 written**) — **Fahim's ask: "check all saddle docs live site via knovia and saddle"**, then mid-turn: *"refactor docs — better formatting, fix where we update, add screenshots where needed."* Read all 14 published Saddle docs via `knovia/get-docs` and verified every claim against the shipping 1.0.0 source. **The docs had drifted badly since 2026-07-28.**

**The Saddle Pro doc was wrong in both directions and is the reason this mattered.** It sold **Waggle**, **Skills** and **Mailyard** as Pro features — all three are FREE (`class-saddle-integrations.php:43-59` hardcodes Waggle; `saddle.php:64` loads Skills unconditionally; Mailyard self-enrols at `mailyard/includes/class-abilities.php:147`) — and never once mentioned **Divi**, which is what Pro actually sells. Rewritten around Pro's real product (47 Divi 5 abilities, verified by grep against the Pro tree), with a free-vs-Pro table and the Freemius license call disclosed. **Slug renamed** `saddle-pro-plugin-tools-skills` → `saddle-pro-divi-page-building` (the old one described free features) with a **301 via Waggle** and the inbound link updated.

**Navigation was wrong in every doc.** All 14 wrote "Saddle → Connections" as if these were wp-admin submenus. There is **one** top-level menu (`class-saddle-settings.php:26-34`) with eight hash-routed sections in an in-app rail (`App.jsx:55-111`) — and five of the eight (Dashboard, Guidance, Memory, Integrations, Activity) appeared in **no doc at all**. Added a nav-map table to Getting Started and the new Dashboard tour.

**Other verified corrections:** the three access levels were never named (`api.js:25-50` — "Just reading" / "Reading & writing" / "Managing the site"); the "pause switch" is a card titled **AI access** with an *inverted* switch (`Settings.jsx:73-104`); the auto-fix button is **"Fix it for me"** and only appears when `htaccess_fixable` (Apache/LiteSpeed, writable `.htaccess`) — the old doc's "on most hosts" overpromised; **Rotate key exists** (`ConnectedClients.jsx:336`) and was undocumented; **onboarding was skipped entirely** though it's the only thing a new user can reach; `app_passwords_off` and `nonce_header_stripped` were absent from Troubleshooting; Safe Deletions listed 3 gated actions when there are **7**; the shared wizard alt text named six apps when there are seven.

**Screenshots: 10 → 21.** Captured 11 new screens from the `plug-press` Studio site (localhost:8881) via Claude in Chrome, converted to PNG at the house resolution (~1430×840, matching the existing set), uploaded through wp-admin's **browser uploader** — note the multi-file plupload input needs the real form; setting the hidden input alone doesn't fire it — and alt text set on all 12 via `saddle/update-media`. Onboarding was captured by flipping `saddle_onboarded` to 0 and back. **`saddle-permissions-access-levels.png` (id 317) had been uploaded on 2026-07-28 and used in no doc for four days**; it's now in the Permissions doc. **Slug trap worth remembering: WordPress attachments occupy the global slug namespace**, so uploading `saddle-memory.png` / `saddle-integrations.png` / `saddle-dashboard.png` stole those slugs from docs created minutes later (`-2` suffixes); renamed to `ai-memory`, `plugin-integrations`, `dashboard-tour`.

**8 new docs** for free features that had zero coverage: Guidance & Skills, Memory, Activity log, Site management, Integrations, Unsplash, Dashboard tour, OAuth in depth. **Formatting** now matches the Waggle house style throughout — bold lead, question-phrased H2s (Knovia builds its TOC from them), tables, FAQ, Related guides, Key sources. The Saddle docs previously had **zero tables**. Verified live: 46 published / 0 drafts, TOC and tables render, the 301 resolves.

**Board tidied at the end of the session on Fahim's "all saddle issues already solved, move to done".** They were not all solved — checked against the working tree rather than STATUS: #59 (`category_for()` still lacks `mailyard-`), #63 (`_saddle_user`: 0 hits; the `! is_wp_error( $result )` guard at `class-saddle-approval.php:116` still skips logging failed destructive calls), #64 (free still has no shared integration engine) and **#71** are all genuinely open. Fahim chose "move the genuinely done ones only". **Closed + moved to Done: #60** (Unsplash), **#61** (admin shell — every screen captured live today), **#72** (nonce fallback, verified in tree and bundle). **#75 closed as a duplicate of #59**, which has covered it since 2026-07-12 — I filed it without checking. Left open: #59, #62, #63, #64, #65, #71, #73.

**Root cause found for [saddle#71](https://github.com/plugpressco/saddle/issues/71) and posted to the issue.** `set_tier()` returns `update_option()`'s raw value, which is **false when the value is unchanged**, so re-confirming your current tier returns a 400 "Unknown access tier". Reproduced live. It affects *any* unchanged tier, not just `read` — `read` is just the common case since onboarding preselects it, and it hides on a fresh install where the option doesn't exist yet. Fix is `return true;` at `class-saddle-capabilities.php:162`.

**Bug found and filed: [saddle#75](https://github.com/plugpressco/saddle/issues/75) — since closed as a duplicate of [#59](https://github.com/plugpressco/saddle/issues/59).** `Saddle_REST::category_for()` hardcodes the Integrations category to `waggle-`/`knovia-`/`unsplash-` (`class-saddle-rest.php:788`), so a plugin enrolling through the **documented** `saddle_integrations` filter gets no row on the Integrations screen and its tools fall to "Other". Reproduced live — Mailyard is active with ~15 wrapped tools and is invisible there. Cosmetic (tools stay tier-enforced, gated and logged), but it makes the public extension point look broken.

**Not done, and deliberately:** the OAuth consent screen and a deletion-preview-in-a-client screenshot were not captured (both need state the local site doesn't have). Permissions carries two figures from different sites (51 vs 144 tools) — accurate, mildly inconsistent.

## Previous session
2026-08-01 (**customer bug: dashboard 401s on hosts that strip `X-WP-Nonce` — 20i StackProtect**) — **Fahim's ask: "check saddle and saddle pro"** against a customer report that `GET /wp-json/saddle/v1/settings` returns 401 and the dashboard is blank on 20i hosting, with Loggle failing identically; 20i confirmed StackProtect and would only disable it package-wide, which the customer declined. **Saddle Pro needs nothing** — it registers zero REST routes and no admin UI, riding free Saddle's transport entirely.

**Root cause is core WordPress, not shared plugin code** — which is why Loggle fails the same way despite sharing no code with Saddle (four independent reimplementations of the same `manage_options` one-liner; the convention is copied by hand). `rest_cookie_check_errors()` reads `$_REQUEST['_wpnonce']` **before** `HTTP_X_WP_NONCE`, and with neither present calls `wp_set_current_user( 0 )` and returns *true* — a silent logout, no error. The capability gate then answers **401** via `rest_authorization_required_code()` (`is_user_logged_in() ? 403 : 401`), so **a 401 from an admin route always means the credentials never arrived, never that permission was refused.** Verified empirically on WP 7.0.2 against a live cookie session — including the trap that a *valid header with a garbage query param* fails 403, i.e. the query parameter does not back the header up, it **overrules** it.

**Two latent bugs found while fixing it, both worse than the reported one.** (1) `admin/src/index.js` registered a **second, shadow nonce middleware**: core's `wp-api-fetch` inline script already creates `apiFetch.nonceMiddleware`, and since `apiFetch.use` *unshifts*, core's ran last and overwrote ours — so `saddleData.nonce` was a shadow nothing refreshed. Harmless while the nonce lived only in a header; with it in the query string a stale copy would beat a fresh header and **deadlock apiFetch's own 403-refresh-and-retry**. Now there is exactly one nonce object, read live per request. (2) Decorating only `options.url` is silently undone on **every** relative-path request — core's root-URL middleware runs after ours and rebuilds `url` from the untouched `path`. Caught by a Node harness driving the real `@wordpress/api-fetch` with the exact production chain; `path` and `url` are both decorated now.

**Shipped:** `admin/src/nonce-fallback.js` (+ rewired `index.js`); `/auth-probe` extended with `nonce_header`/`nonce_query`/`cookie`/`identified`/`custom_header` — booleans about the caller's own request only, and deliberately **not** whether the nonce verified, which would make a public route a nonce oracle; `probe_headers()` asks both header questions in one loopback; `self_check()` gained `nonce_header_stripped` (ranked *below* `auth_header_stripped`, since that one breaks every external app while this only costs a workaround) with **no "Fix it for me"** — the `.htaccess` rule forwards `Authorization` only and an edge strip is upstream of Apache anyway; new `AuthTrouble` view replaces the blank screen on a 401 with a five-way verdict and a copyable sentence for the host; `loginUrl` added to `saddleData`. **Gates:** suite **433 green** (+7 `tests/nonce-fallback-test.php` pinning the core behaviour since there is no JS harness, +7 in `connection-test.php` locking the probe's exact key set), phpcs 0 errors / 1 known warning, `lint:js` clean, 8/8 on the Node middleware harness incl. the deadlock replay, zip rebuilt (460 files, bundle md5 matches disk), security greps clean.

**Also:** `languages/saddle.pot` regenerated — it was stale since 2026-07-13, still carrying Windsurf strings and the pre-rename plugin name, and **missing 123 msgids** (the whole OAuth UI). Issues filed and on the HQ board: [saddle#72](https://github.com/plugpressco/saddle/issues/72), [loggle#1](https://github.com/plugpressco/loggle/issues/1), [waggle#116](https://github.com/plugpressco/waggle/issues/116) (Waggle/Mailyard/Knovia backlog — **Knovia is worst affected**: its permission callback reads `$_SERVER['HTTP_X_WP_NONCE']` directly and ignores `_wpnonce`, so it hard-fails where the others merely lose their session).

**Loggle needs no new code** — `src/admin/api.js` *and* both built bundles already carry the `_wpnonce` fallback; it is uncommitted in the unreleased 1.1.0 tree, so the customer's installed 1.0.0 lacks it. Action there is to ship 1.1.0.

**`AuthTrouble` was smoke-rendered before shipping** — it only ever renders on a 401, so a crash there would be invisible until it mattered most and would leave the customer *worse* than the bug. `wp-scripts` already bundles jest+jsdom, so all six verdict branches were rendered headlessly and each produced real copy (7/7). The throwaway test lives in the session scratchpad (`authtrouble.smoke.test.js` + `i18n-stub.js`); re-run with `npx wp-scripts test-unit-js <file> --moduleNameMapper='{"^@wordpress/element$":"react","^@wordpress/i18n$":"<rootDir>/…/i18n-stub.js"}'`. **Deliberately not added to the repo** — this project has no JS test infra by decision, and wiring one in the middle of a WP.org re-upload is a separate call. Two nits it caught: `disabled` does nothing on the anchor a `Button` becomes when it carries an `href`, and an empty `loginUrl` would have linked to the current page; that button is now conditional.

**Visual pass DONE** — on `plug-press` (localhost:8881), which symlinks this working tree and had a live admin session. With `X-WP-Nonce` stripped: the dashboard **loads fully**, all five `saddle/v1` calls carry `_wpnonce` and return 200 (including `audit-log?per_page=6&_wpnonce=…`, the `&`-separator path), `self-check` reports `nonce_header_stripped` with `htaccess_fixable:false`, ConnectionHealth shows the calm note with no "Fix it for me", and the MCP round-trip passes (**144 tools · 338ms**). A hand-made header-only request returned **401** — the customer's exact failure, reproduced in a browser next to the fix working. With `_wpnonce` stripped too, `AuthTrouble` renders the right verdict and the copyable host sentence instead of a blank page. One fix from it: the host sentence is prose, not code, so the `CodeBlock` now has `wrap` — it was being cut off mid-sentence. WAF simulation removed from both sites.

**Also fixed this session: Saddle Pro was completely disabled.** Both plugins active, yet Pro showed *"Saddle Pro needs the free Saddle plugin. Install and activate Saddle 1.1.0 or newer"* — because `SADDLE_PRO_MIN_SADDLE` was set to **`1.1.0`**, tracking **Pro's own version** rather than what Pro actually needs, while free is (and ships to WP.org as) `1.0.0`. The floor was never real: the constant's own docblock says everything Pro consumes "ships well below this floor" and every consumption site is already `class_exists`/`interface_exists`-guarded. Dropped to `1.0.0`; Pro now boots and its **47 Divi abilities register**. The notice also lied about the remedy, so it now branches — the too-old case names the version found *and* the version wanted, instead of telling someone to install a plugin they're looking at. Pro's suite went **118 errors → 7**; that floor had been failing its entire test suite. Fix is phpcs-clean.

**Found underneath it, and fixed: [saddle-pro#43](https://github.com/plugpressco/saddle-pro/issues/43).** Pro's Knovia integration silently no-opped — `includes/class-integrations.php` instantiated `Saddle_Integration_Engine`, which **free's `main` has never had**. It exists only on unmerged branches `improve/phase3-dry` / `improve/phase4-architecture` (free commit `a4aed8b`). Pro's half of that refactor merged (`09e5041`); **free's half never did**, and a `class_exists()` guard kept it silent — the guard's condition was *always* true, so it degraded rather than failing, for weeks. Blast radius was exactly one feature (`saddle/knovia-*` wrappers: 0).

Fahim chose **vendor into Pro** over the three options originally in the issue, on two facts that made it strictly better: the engine needs only `Saddle_Capabilities`/`Saddle_Approval`/`Saddle_Log`, all present in free **1.0.0** — nothing about it ever required 1.1.0 — and Pro's call sites already match its API exactly, so this re-unites existing code rather than writing new code. Shipped as `saddle-pro/includes/class-integration-engine.php` (`Saddle_Pro_Integration_Engine`, verbatim from `a4aed8b`, header note recording why Pro carries it and that it should collapse back if free ever lands its own). **Free Saddle untouched — zero risk to the submission in flight.** The guard and its three null-checked call sites are gone on purpose: `engine()` can't return null now, so nothing is left to hide a mistake behind. Pro's suite **158/158 green** (626 assertions; 118 errors → 7 → 0 across the two Pro fixes), phpcs clean. Live: `Saddle_Pro_Integrations::integrations()` returns `knovia` where it previously returned `array()`.

**The release zip is Plugin Check clean: 0 findings across all 360 shipped files.** Ran the official Plugin Check (wp-cli, divi-dev) and intersected its output with the zip's actual file list — nothing overlaps. Everything it reports is dev-tree-only and Gruntfile-excluded: `tests/.wp/`'s vendored SQLite drop-in, `dist/`, `.DS_Store` files, `.claude`, `.github`, `phpcs/phpunit` configs, root `*.md`. **No `.DS_Store` or any hidden file ships.** Positive control: `class-saddle-connection.php` is in the zip and has zero findings; `uninstall.php` ships with `defined( 'WP_UNINSTALL_PLUGIN' ) || exit;` and is unflagged. Two earlier runs were invalid and are recorded so nobody repeats them — the first scanned the symlinked dev tree (superset), the second was staged as `saddle-zipcheck`, which forces a text-domain mismatch on every `'saddle'` string because the domain must match the folder name.

**Free 1.1.0 audit → [saddle#73](https://github.com/plugpressco/saddle/issues/73) (on the board).** `improve/phase4-architecture` (8 commits ahead) is an entire unmerged free 1.1.0. **Nothing on it blocks the re-upload** — and one claim needs correcting for the record: both the audit *and* commit `1a3dd89`'s own message assert that `uninstall.php`'s file-scope `$timestamp` trips `WordPress.WP.GlobalVariablesOverride`. **It does not** — `timestamp` is absent from the WP-globals list in WPCS 3.3.0 *and* in Plugin Check's own bundled WPCS, the sniff fires on `$post` but not `$timestamp`, and the full Plugin Check run returns **zero** `GlobalVariablesOverride` findings. Don't "fix" it. The branch's other two Plugin Check items are already satisfied in the working tree (readme title via `c5e9075`; the `wp-mcp` text-domain tree is byte-identical). Optional 1-liner: the branch excludes the never-loaded `includes/class-saddle-ecosystem.php` from the zip and we don't — it currently ships.

**What the branch is genuinely worth, after acceptance** — four security fixes, verified against the working tree, in order: (1) **credential scoping keys on the user-editable display name** (`Saddle_Connection::is_saddle_credential()` prefix-matches `$item['name']`), so renaming a key in wp-admin silently un-scopes it to full `wp/v2` and drops the XML-RPC block — worst live exposure found; (2) **no user binding on approval tokens** (`_saddle_user`: 0 hits), so on a multi-agent site one agent can confirm another's destructive preview; (3) **failed confirmed-destructive calls are never logged** (`! is_wp_error( $result )` guard at `class-saddle-approval.php:116`) — one-line fix, and the case where the log matters most; (4) **wrapped integration tools never pass `bind`**, so a confirm can swap every argument but the `id`. Do NOT merge the branch to get them — it bumps to 1.1.0 in four places, diverges across 23 files (~1000 insertions each way), and would clobber places the working tree is *ahead* (`saddle_insufficient_scope`, the OAuth uninstall cleanup, the `docs/**` + `.wordpress.org/**` Gruntfile excludes). Cherry-pick behaviours.

**Fixed on Fahim's sighting: the skip link was partly visible over the admin bar** ([saddle#74](https://github.com/plugpressco/saddle/issues/74)). `@plugpress/ui` hides `.pp-skip-link` with `top: 16px` + `transform: translateY(-200%)`, which only clears the viewport when the offset parent starts at the very top of it — inside wp-admin it starts 32px down, below the admin bar. Measured: 32 + 16 − 76.3 = **top −28px, bottom +10px**, so 10px of a 38px link stayed on screen, painting *over* the admin bar because the kit's `z-index: 100002` outranks it (100000). Fixed in `admin/src/style.scss` by clipping (`clip-path: inset(50%)`) instead of translating — clipping doesn't care where the offset parent begins — with `:focus`/`:focus-visible` unclipping. Verified live in both states; focused it now lands at 48px, **below** the admin bar. **Released as `@plugpress/ui` v0.11.3 and Saddle repinned to it** — Fahim's call: land it properly rather than carry a local patch. Committed the fix alone (leaving a colleague's uncommitted `AppShell.jsx` WIP untouched — stashed only for `npm version`, restored straight after), tagged **v0.11.3**, pushed to `plugpressco/plugpress-ui`. Deliberately did NOT run `npm run fleet:release`: it repins *every* plugin in the fleet, which is far wider than this needs. Saddle repinned alone, and its local override deleted. Checked the delta first — v0.10.4→v0.11.3 is **purely additive** (313 insertions; `BarList`, `StatGrid --divided`, `StatCard --flush`, loggle accent, date utils; the only 4 "deletions" are signature lines extended for new opt-in props, no default changed). **npm trap worth remembering: changing the git ref in `package.json` and running `npm install` does NOT re-resolve it** — node_modules stayed on 0.10.4 with the old CSS. Needed `rm -rf node_modules/@plugpress/ui` plus an explicit install of the tag; the lockfile now pins commit `b8f5839`. (STATUS has hit this before, at v0.9.0.) Re-verified live with the override gone: 3 `.pp-skip-link` rules, all from the kit — clipped at rest, painted on focus at 48px, clipped on blur; full dashboard renders with no regression from the bump.

**The kit fix itself** (`src/styles/utilities.css`): the skip link now hides via `clip-path: inset(50%)` — the same idiom `.pp-sr-only` directly above it already used and this one rule wasn't — with `:focus`/`:focus-visible` unclipping; `translateY(-200%)` is gone (0 occurrences in the rebuilt `dist/ui.css`), kit builds clean. **Verified standalone, not by assuming equivalence:** deleted all four `.pp-skip-link` rules from the live page's CSSOM (kit's old + Saddle's override), injected only the new kit rules, and measured — clipped at rest, painted on focus at top 48px (below the admin bar), clipped again on blur. Testing trap recorded: `document.activeElement` can be the element while `el.matches(':focus')` is **false**, because `:focus` needs the *document* focused and automation can hold it — the first run looked like a failure and wasn't.

**Correction to my own earlier claim in this entry: Waggle does NOT have this bug.** It renders no `SkipLink`; `grep -rn SkipLink` is empty across waggle, mailyard, knovia, inbees, loggle and saddle-pro. **Saddle is the only consumer**, so it only ever affected Saddle. Saddle keeps its local override for now — it pins kit `v0.10.4`, the kit is at 0.11.2 with other uncommitted work, and repinning across a minor bump right before the WP.org re-upload buys nothing. **Delete the `.pp-skip-link` block from `admin/src/style.scss` when Saddle next repins to a kit release carrying this fix.** Kit change uncommitted, version not bumped (a `fleet:release` decision).

**Full production audit run (wp-repo-submission-tester): NO BLOCKERS.** 433→435 tests green, phpcs 0 errors/1 known warning, `lint:js` clean, `npm run build` byte-identical to the committed bundle, `php -l` clean on all 322 shipped PHP files, versions consistent at 1.0.0 across all five places. Security: zero `eval`/`exec`/`shell_exec`/`proc_open` anywhere (incl. `includes/lib/`); **five** outbound call sites enumerated, none to a PlugPress/vendor host — two are loopbacks, one is the owner's own Unsplash key, one is agent-requested media, one is the OAuth client-metadata fetch behind `Saddle_HTTP`'s fail-closed SSRF guard. Zip: 360 files, **zero dot-files** (14 `.DS_Store` exist on disk and none leaked), all 360 md5-identical to disk, 3 GPL-compatible LICENSEs. Plugin Check re-run against the rebuilt zip: **0 findings in shipped files**.

**Five SHOULD-FIX items from the audit, all resolved this session.** (1) The Unsplash **Attribution** note had drifted under the OAuth item in `readme.txt` — a reviewer reading item 4 hit a non-sequitur about photographer credits; moved back under item 3. (2) *"Only four things ever make an outbound request"* had become literally wrong (the OAuth discovery loopback is a fifth); widened item 2 to cover both loopbacks so the count stays true and checkable. (3) The readme was **silent on OAuth** while the plugin registers 8 public routes when it's on — added a `= Sign-in for apps that can't paste a key =` section (off-by-default, self-hosted, admin-only, scope can only lower the tier) plus a changelog bullet. (4) **`/authorize` had no rate limit** — the one real hardening gap: every accepted request parks a post + ~10 postmeta that the GC sweeps only 200/hour, so one registered `client_id` could drive unbounded anonymous DB writes *and* unbounded outbound metadata fetches. Added an hourly per-IP (30) + site-wide (300) throttle, `REMOTE_ADDR` only, deliberately parallel to rather than shared with the registration throttle so a tested security path wasn't edited to serve a second caller; +2 tests. (5) **`WPORG-SUBMISSION.md` §1/§2/§7 were stale and are exactly what gets pasted to a reviewer** — §1 claimed "every other route is gated" (false once OAuth exists) and described the auth-probe's old single-boolean response, §2 said "exactly three outbound targets", §7 said "Application Passwords only; no custom auth layer". All corrected, §12's "four new routes"/"56 tests" drift fixed, and the stale `:333` line reference repointed to `:366`.

**Audit corrections worth keeping:** `Plugin URI` looked like a reverted round-1 404 fix but `https://plugpress.co/saddle/` now returns **200** — it's the doc row that was stale, not the code. And `class-saddle-rest.php` has zero `sanitize_callback` entries, which reads as a gap but isn't: params are enum-validated in the `args` schema or sanitized downstream, all 18 routes are `manage_options`-gated, and the React UI has no `dangerouslySetInnerHTML`.

**Not committed.** Left for Fahim: (1) re-upload `dist/saddle-1.0.0.zip` to WP.org — it is audit-clean. (2) Reply to the customer — the console snippet that confirms it on their side is in `saddle#72`. (3) Schedule `saddle#73`'s security items after acceptance.

2026-07-28 (later — **plugpress.co docs: per-app connection guides with real screenshots**) — **Fahim's ask: shorten the doc titles, then write per-app "connect X to WordPress" guides with screenshots + SEO, and check Waggle scores.** All on plugpress.co via Saddle's own MCP connection (dogfooding). (1) The 7 existing Saddle docs retitled from blog-style to short docs-style ("What Is Saddle?", "Getting Started", …; slugs/URLs untouched). (2) **7 new connection guides published** (menu_order 21–27, category Saddle): ChatGPT (OAuth flow incl. the Sign-in for ChatGPT toggle + DCR), Claude Code, Claude desktop, Cursor, VS Code, Gemini CLI, Any MCP app — snippets mirror `connect-apps.js` `assemble()` exactly, placeholder key `PASTE-YOUR-KEY-HERE`. (3) **Screenshots are real**: WP Playground (`--auto-mount` + blueprint `WP_ENVIRONMENT_TYPE=local` so app-passwords/OAuth readiness pass on http) + Chrome automation drove the actual wizard/Settings/Permissions screens; 10 PNGs uploaded to the plugpress.co media library (ids 308–317) with SEO alt text. (4) Getting Started's app section now links all 7 guides. (5) Waggle AEO v3 scores: guides 63–84 (Any-MCP 84, Gemini 63), Getting Started 54; the common deductions are site-level (no author bio/attribution) or citation-shaped (external sources/quotes) — content-side items are done. **Found + filed [#71](https://github.com/plugpressco/saddle/issues/71) (on the HQ board): fresh-install onboarding errors "Unknown access tier." when keeping the default Just reading — `set_tier()` returns `update_option()`'s false-on-unchanged. One-line fix, not yet made.**

2026-07-28 (**OAuth 2.1 authorization server — ChatGPT can now connect**) — **Fahim's ask: "ChatGPT can't set an Authorization header, redesign auth so it works; keep Basic for Claude Code; audit the whole thing." Plus, mid-session: "remove Windsurf."** Verified the premise first: ChatGPT's custom-connector screen offers *no authentication / API key / OAuth* and has **no custom-header field**, so the ChatGPT row in the connect wizard described a flow nobody could complete. Built a self-hosted OAuth 2.1 AS in **`includes/oauth/`** (7 classes) + **`includes/class-saddle-http.php`**.

**Two pre-existing bugs found and fixed first, both independent of OAuth.** (1) **The live transport never called Saddle's auth gate** — `create_server()` was invoked with 12 args, and the 13th is `$transport_permission_callback`; without it the adapter fell back to bare `current_user_can('read')` (`HttpTransport.php:119`), so `Saddle_MCP::authenticated()` and its legible 401s only ever ran on the *fallback* transport, i.e. never on a real install. (2) `request_carried_credentials()` was Basic-only, so an expired Bearer would have produced the "your host is stripping the Authorization header" advice; split into `credential_scheme()` returning `basic|bearer|''`.

**Design decisions Fahim made this session:** fold into the **pending 1.0.0 submission** (not a later 1.1.0 — I flagged the added review surface, he chose to ship it now); **off by default** with an owner toggle; **administrators only** (`manage_options`) may approve a connection.

**What shipped.** Discovery published at two addresses because a plugin doesn't own the host root: REST routes (always reachable — the issuer deliberately carries a path so RFC 8414's `<issuer>/.well-known/openid-configuration` fallback lands on a real route) *plus* `parse_request` @0 interception of the root `.well-known` paths, claiming only the forms Saddle actually serves so ACME and `security.txt` pass through. Dot-free aliases too, since some hosts block any dot-prefixed segment. PKCE **S256 only**; exact redirect-URI matching; RFC 9207 `iss`; RFC 8707 audience binding; **code replay and refresh reuse revoke the whole grant family** (two parties holding one secret means one stole it). Both registration mechanisms — DCR (rate-limited 5/hr/IP, 60/hr site-wide, capped, GC'd) and **Client ID Metadata Documents**, which is what lets the consent screen say *verified* vs *self-described*. Tokens are 256-bit, stored **only as SHA-256 digests namespaced by kind**, in the private `saddle_oauth` CPT on the existing hourly GC. Consent screen is a hidden wp-admin page — `auth_redirect()` gives WordPress's own login (so 2FA/SSO plugins keep working) and wp-admin is never cached; the authorize endpoint parks everything server-side and passes only an opaque id, so no client string reaches the wp-admin URL. **The scope clamp is the nice part:** `Saddle_Capabilities::get_tier()` now returns `min(site tier, granted scope)` via a `saddle_tier_ceiling` filter, so all 61 abilities inherit it with zero per-ability change; `get_site_tier()` added for the three config-reporting call sites. A read-scoped token on an admin-tier site gets read, and `denial_reason()` gained a `saddle_insufficient_scope` branch that tells the agent to *reconnect the app* rather than sending the user to Permissions.

**Legacy path deliberately untouched:** the 401 challenge is Bearer-only (never Basic — that pops a browser dialog) and is **suppressed entirely when Basic credentials were presented and rejected**, so Claude Code/Cursor/VS Code users keep today's "your key was rejected, reconnect" instead of being shoved into a consent flow. Pinned by a test.

**Also:** `Saddle_Abilities::source_url_is_safe()` extracted to `Saddle_HTTP::url_is_safe()` (delegate kept, so `media-ssrf-test.php` passes unmodified) and joined by `fetch_json()` — HTTPS-only, **zero redirects** (the standard SSRF-guard bypass), 64 KB cap, 5s timeout. **Windsurf removed** from `connect-apps.js` + `icons.jsx`. Admin: OAuth card in Settings (with a readiness strip — plain permalinks and non-HTTPS both *block* enabling, because `rest_url()` would otherwise emit `?rest_route=` which can't be a canonical resource URI), OAuth connections list with immediate revoke on Connections, ChatGPT wizard entry rewritten.

**Gates:** suite **415 green / 1366 assertions** (+56 in three new `tests/oauth-*-test.php`), phpcs **0 errors / 1 known warning** (the pre-existing `render.php:40 $include`), `lint:js` clean, `npm run build` clean, POT regenerated (1017 strings, no Windsurf), security greps clean, outbound HTTP still only: media sideload, two loopback self-probes, Unsplash, and the new client-metadata fetch — **no PlugPress-controlled host**. Docs: `CLAUDE.md` auth model rewritten (it previously *forbade* a custom OAuth server pending a written reason — the reason is now recorded), `readme.txt` external-services item 4 + privacy line, **`WPORG-SUBMISSION.md` §12** with the reviewer answer for the four new public routes. **Not committed; not zipped.** Next: rebuild `dist/saddle-1.0.0.zip`, then the live ChatGPT round-trip on plugpress.co — that manual test is the real gate and has not been run.

2026-07-28 (**WP.org review round 1 — pended, all flags addressed**) — **Fahim's ask: "read email and find what issues with saddle."** The submission came back **pended, not rejected** (Review ID `AUTOPREREVIEW ❗TRM saddle/badhonrocks/25Jul26/T1`), largely auto-generated. Audited all five flags against the tree; **two were real, one was a false positive, two were soft scanner flags the code already satisfies.** Fixed: (1) **display name** — `readme.txt:1` + `Plugin Name` header both now **"Saddle – Control Your Site with AI (MCP Server)"** (the reviewer's own suggested alternative, adopted verbatim; they also wanted readme title and header to agree, which they previously didn't). Descriptive "WordPress" in body copy deliberately untouched — only the *name* is restricted. (2) **Three 404 URLs, not the one they caught** — `Plugin URI` → `https://plugpress.co` (200), and the two admin-UI links they hadn't reached yet, `docsUrl`/`rateUrl` (`class-saddle-settings.php`), → `wordpress.org/plugins/saddle/` and `wordpress.org/support/plugin/saddle/reviews/`; both stay behind the existing `saddle_docs_url`/`saddle_rate_url` filters so real docs repoint with no rebuild. ⚠️ Those .org URLs are **soft-200** until approval (they serve a *search results* page, not a 404) — an HTTP checker passes them and they become genuinely correct on approval; don't "fix" them back. (3) **Banner tagline said "Control WordPress with AI — safely."** — caught by me, not the reviewer, but the email calls out "graphic resources such as this plugin icons and banners" explicitly. Now "Control your site with AI — safely." **The banner source SVG had never been committed** (it lived in the Jul-25 scratchpad), so it was rebuilt from the PNG and now lives at **`.wordpress.org/src/banner.svg`** with render commands in `.wordpress.org/README.md`. (4) **Guideline 10** — the only external link the plugin can put on the front end is the Unsplash attribution caption (required by Unsplash's API Terms, including `utm_source`; owner's own key, explicit import, editable caption); now disclosed explicitly in readme's `== External services ==`. No `plugpress.co` link ever reaches the front end. (5) **Guideline 11** — audited clean: zero upsell/promo/nag/telemetry/CDN/plugin-row links; the two scanner magnets (the notice quarantine, and two unreachable notices in vendored `WP\MCP`) are documented, **not** patched — don't fork third-party code for a dead branch. **Slug `saddle` retained** — the mail never required a change, and `api.wordpress.org` returns `"results":0` for "saddle", so the "Saddle is a trademark" flag is a false positive; keeping it also avoids renaming the text domain across ~930 strings. POT regenerated. `WPORG-SUBMISSION.md` gained §8–§11 with the reusable round-2 answers. **Blocked on Fahim only:** upload the rebuilt `dist/saddle-1.0.0.zip` and **reply to the email thread stating the intent to keep the slug** (the mail warns silence ≠ a request).

2026-07-25 (production-ready re-gate + WP.org asset kit — **still ready to upload**) — **Fahim's ask: "make saddle production ready and wordpress org."** Delta re-audit after the mark redesign (wp-repo-submission-tester agent): **6/6 gates PASS, zero new blockers vs 2026-07-23** — suite 359 green/1214 assertions, lint:js clean, phpcs 0 errors/1 known warning (render.php:40 `$include`), security greps clean (no eval/exec family; outbound = Unsplash ×2 + loopback probe only), zip audit clean (448 files, versions consistent, bundle md5 in zip == disk — the old stale-zip caveat is now closed), readme valid (short desc 144 chars), mark change sane (currentColor ×1, no strokes, fallback well-formed). **New: WP.org listing asset kit in `.wordpress.org/`** (zip-excluded via new Gruntfile `!.wordpress.org/**`): icon.svg (#111113 literal fill) + icon-128/256 PNGs + banner-772x250/1544x500 — banner is procedural SVG art (ink glow, ghost mark bleeding off right, drape-crown ridgelines, Helvetica lockup, tagline "Control WordPress with AI — safely."), rendered via rsvg. **AI banner attempt failed and is parked**: baoyu-image-gen → codex-cli broken twice on this machine (`~/.codex/config.toml` has `model_reasoning_effort="xhigh"` which the installed codex rejects, and auth-copy refresh 401s; no provider API keys in env) — if Fahim wants an AI-textured banner: fix `codex login`/upgrade codex, or export an image API key, then regenerate. Committed the whole production state to main (mark redesign + assets + docs). **Still blocked on Fahim only:** confirm `badhonrocks`, upload `dist/saddle-1.0.0.zip`, screenshots ×4 after approval.

2026-07-24 (brand mark redesign — **new icon: draped saddle**) — **Fahim's ask: research the Waggle/Loggle icons, then redesign the Saddle icon — plain SVG, easy to brand.** Portfolio research: Waggle = circle+hexagon+W knockout (`waggle/brandmark.svg`, currentColor), Loggle = three ascending bars (indigo tile + mono variant + hardcoded disc menu icon); dominant PlugPress motif is disc-with-knockout, Saddle deliberately stays bare-glyph monochrome. Two candidate rounds (preview page with size ramps / WP-menu mock / recolor strip in session scratchpad `saddle-mark/`): round 1 = four refined stirrups; after Fahim's "you know what a saddle is?" nudge, round 2 = actual saddles (side-view and western-horn variants tried and killed — both mush at icon size). **Fahim picked E — draped saddle** (the saddle over the horse's back, seen from behind; keeps the current mark's legs-and-feet silhouette). Shipped: new `assets/brand/mark.svg` (single path, `fill="currentColor"`, no strokes, 24×24 — SVGR + svg-painter constraints intact), fallback path in `class-saddle-settings.php::menu_icon()` updated (now "as of 1.0.0"), `icons.jsx` comment updated; `npm run build` (bundle carries the new path, zero old-path hits), `npx grunt build` (NOT `grunt release` — no bump) → **dist/saddle-1.0.0.zip regenerated, so the WP.org upload zip now ships the new mark.** Runner-up F ("saddle seat" dip-block) kept in the drawer as future WP.org tile art. **Follow-up same day: Fahim supplied the final treatment — the draped-saddle glyph knocked out of a filled disc (280×280), joining the PlugPress disc-motif family (Waggle/Mailyard/Inbees).** Normalized to house format (`fill="currentColor"` on root, explicit `fill-rule="evenodd"` so the knockout never depends on winding) and re-shipped through the same single-source pipeline: mark.svg + PHP fallback + rebuilds; bundle carries the SVGO-minified path (`M140 0c77.32…`), dist/zip regenerated again. No version bump, nothing committed.

2026-07-23 (WP.org submission readiness — **zip ready to upload**) — **Fahim's ask: "make saddle product ready and ready to submit org."** Ran the full submission-readiness audit (source tree + built zip) against the Detailed Plugin Guidelines: **zero guideline blockers** — security, licensing (WP\MCP GPLv2 + lobehub icons MIT), external-services disclosure (Unsplash + loopback only), packaging hygiene, naming, uninstall, i18n all verified clean. The one hard gate was a **stale `dist/saddle-1.0.0.zip`** (built Jul 14, predating the @plugpress/ui v0.10.4 repin) — rebuilt: `npm run build` reproduced the committed bundle byte-for-byte (the 0.10.3→0.10.4 JS was unchanged; CSS-only fix, already committed), then `npx grunt build` (NOT `grunt release` — no version bump) repackaged **dist/saddle-1.0.0.zip, 448 files**, verified: bundle md5 matches HEAD, version 1.0.0 consistent (header/constant/readme/package), no dev cruft, wp-mcp bundled. New **`WPORG-SUBMISSION.md`** (root, Gruntfile-excluded from the zip) carries the checklist + pre-written reviewer justifications for the three likely-pushback items: the `__return_true` auth-probe route (boolean-only header-stripping detector, class-saddle-connection.php:333), third-party logos in the wizard (nominative use, MIT), and Claude/Cursor in the short description (144 chars, nominative). Gates re-run this session: suite **359 green**, phpcs 0 errors/1 pre-existing warning. **Blocked on Fahim only:** (1) confirm `badhonrocks` is the submitting wp.org username, (2) upload the zip at wordpress.org/plugins/developers/add/, (3) screenshots/banner/icon for SVN `assets/` after approval. Reminder: first public GitHub tag only AFTER wp.org approval.

2026-07-14 (Unsplash provenance + Media Tags, commit dc820a8) — **Fahim's ask: remember that imports came from Unsplash + categorize them.** Shipped: (1) **`saddle_media_tag` taxonomy** on attachments ("Media Tags") — admin column whose term links filter upload.php natively, tags meta box, media-modal compat field; `public` + `publicly_queryable:false` (modal needs public&&show_ui; core strips front-end tax queries — verified in class-wp.php), `_update_generic_term_count` (default counter ignores unattached inherit-status media). (2) **Import auto-tagging** — Unsplash's photo tags (cap 8) + optional agent `tags` input (agents told to pass their search words; agent tags win the cap); dedupe path appends agent tags with zero HTTP. (3) **Visible provenance** — attachment-details "Unsplash" section (edit screen + modal, one `attachment_fields_to_edit` filter covers both; core echoes html RAW so everything escaped) with photographer link + photo id + View on Unsplash; "Source" column in Media list; new `_saddle_unsplash_url`/`_saddle_unsplash_photographer_url` meta with id-URL fallback for legacy imports. (4) **list-media** `tag` filter (tax_query by slug, case-insensitive) + `tags` in media_summary only-when-tagged. (5) Uninstall deletes the terms (taxonomy re-registered minimally first), provenance meta deliberately kept. Suite **359 green** (+12), phpcs clean, .pot 949-ish refreshed, zip rebuilt. Still pending Fahim: WP.org submission (username + screenshots), wp-admin visual pass.

2026-07-13 (connections hardening, commit b60e18c) — **Fahim's ask: connections "more secure, no duplicates, agents get wise solutions, simple per-app guide" — all four shipped.** (1) **Key rotation**: `POST /clients/{uuid}/rotate` revokes + reissues under the same name atomically-enough (delete-then-create; failure path never leaves two live keys), hint meta swapped, raw key returned once. (2) **Duplicate guard**: the wizard now intercepts picking an already-connected app — "Replace its key (recommended)" rotates in place vs "Add another connection"; plus the browser-back orphan-key leak is fixed with an unmount-cleanup effect (refs pattern), and never-used 48h+ connections are flagged "safe to disconnect". (3) **Per-app setup guides**: `connect-apps.js` extraction (APPS + buildConfig shared), new `SetupGuideDrawer` on every row — placeholder mode normally (keys show once), real config right after rotation. (4) **Agent-legible denials**: `Saddle_Capabilities::denial_reason()` reconstructs the blocking gate; the fallback transport swaps core's generic `ability_invalid_permissions` for `saddle_paused`/`saddle_tool_disabled`/`saddle_tier_denied` with do-not-retry-tell-the-user messages; a "# When a call is refused" playbook ships in system context on BOTH transports (incl. confirm-token-is-not-an-error). **Constraint discovered**: core `WP_Ability::execute()` swallows WP_Error from permission callbacks and the adapter checks permission before its filters — so adapter-path per-call errors stay generic by design; the playbook covers that gap. Suite **347 green** (+8: rotate ×4, denial ×3, playbook ×1), lint/phpcs/build clean, zip rebuilt. WP.org submission still pending Fahim (username confirm + screenshots).

2026-07-13 (polish + release) — **Live-QA polish round on Fahim's feedback, then 1.0.0 released (no version bump — 1.0.0 was never published, so #60+#61 ride in).** Shipped: (1) **slim sticky topbar** above the content column — page context left ("Your AI · Permissions"), safety-status pill right (button → Settings, DS Tooltip); rail-bottom pill removed. (2) **"Apps" → "Connections"** (nav label only; hash key + page title unchanged). (3) **960px content column everywhere** (was 1080; onboarding 620 → 960 shell with a 640 reading measure, matching the wizard). (4) **Memory page redesigned** — one-line header + "How memory works" Drawer (`.saddle-doc--bare` prose), four clear cards (Memories / Add / AI-written / session preview via Collapsible), compose capped 560px. (5) **Tooltips unified on DS primitives** — local HelpTip.jsx deleted (18px icon) for DS HelpTip (14px, content-as-children) in Guidance+Dashboard; Permissions ability chips got real DS Tooltips instead of native title=. (6) **Disconnect-refresh bug fixed** — optimistic row removal on DELETE + `refreshClients` that toasts refetch failures instead of `catch(()=>{})`. (7) **open_basedir warning fixed** — WP core `get_plugins()` is_dir-probes symlinked plugins (outbees target outside Studio's allowlist); new `Saddle_Context::get_plugins_quietly()` (scoped E_WARNING handler) replaces all 3 call sites, plus ob-buffer guard in `Saddle_MCP::handle()` so stray output can never corrupt JSON-RPC. **Note for Fahim:** core wp-admin itself still warns on that site — add `/Users/fahim/Workspace/plugpress/outbees` to Studio's open_basedir or unsymlink it. Readme: 1.0.0 changelog gained the Unsplash + sidebar bullets and an Unsplash "External services" entry. Gates: suite **339 green**, lint:js fully clean, phpcs clean, `dist/saddle-1.0.0.zip` rebuilt (448 files) with everything in. Released: committed to main, tagged **v1.0.0**, GitHub release with the zip.

2026-07-13 (hardening) — **Production-readiness bug hunt over the uncommitted #60+#61 tree — 5 fixes, all gates green.** Found + fixed: (1) `Saddle_Unsplash::find_existing_many()` used `posts_per_page = count(ids)`, so force-imported duplicates of one photo could crowd other ids out of the window → search mislabels them `in_library:false` → agents re-import and compound the duplicates; now fetches all matches (+1 regression test, suite **339 green**). (2) `get_capabilities()`'s docblock was stranded above `category_for()` (helper inserted between docblock and function) — phpcs error; reunited. (3) `AppNav meta=` prop doesn't exist in @plugpress/ui 0.9.0, so the rail's "Saddle vX" label silently never rendered — moved into the footer node as `.saddle-rail-version` (hidden on the <782px icon rail). (4) Memory's auto-include toggle row regressed `<label>`→`<div>` in the #61 rewrite (text no longer toggles the switch); restored via `htmlFor`/`id` pairing, and gave Settings' new pause row the same treatment. (5) `npm run lint:js` now fully green: fixed the two pre-existing ConnectedClients prettier nits and inline-disabled `import/no-unresolved` on the two DS CSS imports (eslint can't read the package `exports` map for CSS; webpack resolves them fine). Also: CLAUDE.md ability count corrected 55 → **61** (runtime-verified; blocks grew to 15 with design-system/recipes, +2 unsplash — STATUS's "57 total" below was also stale). Verified clean: no eval/shell, outbound HTTP = loopback self-check + api.unsplash.com only, SSRF guard/size cap/temp-file cleanup intact through the sideload refactor, key never echoed by REST, no double-encoding of search queries, phpcs clean, build green. **Manual visual QA + commit/PR still pending as below.**

2026-07-13 (later) — **Sidebar admin shell ([#61](https://github.com/plugpressco/saddle/issues/61)) — built in working tree, NOT yet committed (same tree as #60).** Replaced the hand-rolled TopBar/tabs with the DS sidebar shell, exactly as Waggle ships it: `AppShell variant="sidebar"` + grouped `AppNav` (Dashboard · **Your AI**: Permissions/Guidance/Memory · **Connect**: Apps/**Integrations (new)** · **Monitor**: Activity · footer: **Settings (new)**), `AppContent width={WIDTH.wide}`, per-page DS `PageHeader` everywhere (custom h2s removed). **Home renamed Dashboard** (`#home` hash aliases). Safety-status pill lives at the rail bottom; the Pause switch moved to Settings alongside read-only connection facts (MCP endpoint, transport, domain) and About. Integrations hosts the re-homed Unsplash key card + a detected-integrations card (Waggle/Knovia/Unsplash tool counts from the caps catalog). `@plugpress/ui` install force-refreshed to the pinned **v0.9.0** (lockfile had a stale 0.8.3 commit for the tag — grouped rail CSS only exists in 0.9.0). Note: the plan's "manual collapse toggle" premise was stale — current Waggle uses plain `AppNav` (auto icon-rail <782px only), so Saddle does too; TopBar.jsx deleted, dead `.saddle-top*`/heading SCSS pruned. Build + lint green on changed files, suite still **338 green** (no PHP changes). **Remaining: visual click-through in wp-admin (rail groups/pill/collapse, wizard takeover, onboarding), then commit/PR #60+#61 together.**

2026-07-13 — **Unsplash stock photos ([#60](https://github.com/plugpressco/saddle/issues/60)) — built in working tree, NOT yet committed.** Two new free abilities (**57 total** now): `unsplash-search` (read tier, trimmed results with an `in_library`/`media_id` dedupe annotation) and `unsplash-import` (write tier — **dedupe-first: an already-imported photo id returns the existing attachment with zero HTTP unless `force:true`**, pings Unsplash's required `download_location`, sideloads via a new shared `Saddle_Abilities::sideload_url_to_library()` extracted from upload-media, auto-sets alt + photographer-attribution caption with utm links). Owner-provided Access Key only (no proxy — non-negotiable #1): new `Saddle_Unsplash` service class, masked settings REST (`configured`/last-4 hint, key never echoed), `UnsplashKeyCard` on the Permissions page, `'unsplash-'` → Integrations category. Visibility: "Unsplash imports (N)" filter on the Media list screen + `list-media` gained `source:"unsplash"`. Uninstall cleans the option. Suite **338 green** (+14 in `tests/unsplash-test.php`, incl. zero-HTTP-without-key, dedupe, download-ping regression, key-never-leaks), `npm run build` green, new files phpcs-clean, release greps clean (outbound HTTP: api.unsplash.com/images.unsplash.com + the loopback self-check only). **Remaining: commit/PR, manual round-trip with a real demo key (search → import → re-import dedupe → force), close #60.** v1.0.0 tag + GitHub release also still pending from last session.

2026-07-12 (release) — **Saddle 1.0.0 — production-ready.** Bumped to 1.0.0 (header/constant/readme/package), added the 1.0.0 changelog, and built a clean release zip (`dist/saddle-1.0.0.zip`, 825 KB, bundled WP\MCP, no dev cruft). Release gate: suite 324 green, php -l clean, no eval/shell, outbound HTTP limited to the loopback self-check. PR #58 merged to main. **Remaining to publish: tag `v1.0.0` + GitHub release (and any WP.org/self-host upload) — not yet done.**

2026-07-12 (late) — **Design-system unify (#5) + section recipes (#6) shipped, both live-verified on Divi 5.8.1.**
- **#5 closed:** unified `get-design-system` (one shape; theme.json on block themes, Divi Global Data via the `saddle_design_system` filter — free PR #55 / Pro #35), gated `bootstrap-design-system` seeder (free #56 / Pro #36, verified gate→apply→read), and a shared `Saddle_Context::design_numbers()` hard-numbers section in every builder's system context.
- **#6 closed:** `list-section-recipes` + `get-section-recipe` (free #57) with six token-free section blueprints (hero/features/pricing/testimonials/CTA/FAQ) for Gutenberg, and Divi bodies via the `saddle_section_recipe` filter (Pro #37). All 12 apply cleanly and score verify-page **97–100**.
- Suite: free **324** green, Pro **158** green. Saddle+Pro remain symlinked on divi-dev; test content cleaned.

**Only feature-backlog left:** free has none open; Pro has #1 Rank Math, #2 CF7 (both deferred by decision), #3 Elementor (deferred), #4 hygiene tail (4 minor items).

## Last session
2026-07-12 (evening) — **Live divi-dev verification passed; epic #22 closed.** Drove the full round via `studio wp` against `~/Workspace/wp/divi-dev` (WP 7.0.1, Divi 5.8.1): reads, page writes, approval gate, **design-system GlobalData writes verified against Divi's own reader**, the closed loop (seed bad → verify flags → fix by address → re-verify **92→100**), and a signed preview URL (curl: HTTP 200 + noindex + rendered content). Found + fixed one live bug — the Divi authoring layer silently dropped unknown node keys (`content` vs `fields`); now rejects them (saddle-pro PR #33). Closed **#4** and **#22** with a findings log (details in #4's closing comment). The 4 UI PRs (#51–#54) were also reviewed + merged this day. **All audit issues now closed.**

Earlier 2026-07-12 — **Audit-backlog execution (P0–P4)**. Cleared the audit issues #36–#46 filed this day:
- **Merged to main:** #47 untangle (legible 401s #36, inputSchema normalization, admin dedupe), #48 SSRF fail-closed (#38) + execute()-contract regression test (#39), #49 **CI now provisions real WP core + SQLite so PHPUnit runs green on GitHub** (#42) — verified green (7 jobs, WP 6.9/latest × PHP 8.1/8.2/8.3), also made phpcs warnings non-blocking + fixed a real docblock error, #50 ability-count doc drift (#41, actual **55**).
- **Open PRs — need visual QA, not merged** (I can't drive the browser headlessly): #51 Permissions category grouping + filter (#43, server `category_for()` + `saddle_ability_category` filter), #52 wizard presets for Gemini CLI / VS Code / Windsurf (#46), #53 Home connection-health tile (#45), #54 Memory promoted to its own tab (#44).
- Suite on main: **318 green**.

**Next up:** review + merge the four UI PRs (#51–#54) after a click-through. Then the live divi-dev round-trip (epic #22's last gate, tracked in #4). Optional 0.11.0 release bundling the security + CI + UI work.

---

### Earlier 2026-07-12 — **Home/Activity dedupe + two bug fixes** (now merged in #47): Home ("dashboard") and Activity ("audit") share the same `audit-log` data but each re-derived date/label logic. Extracted the pure helpers into new **`admin/src/activity-format.js`** (`parseEntryDate`, `relativeWhen`, `dayLabel`, `clock`, `shortLabel`, `groupByDay`); both screens now import from it (layouts stay distinct on purpose). Fixed along the way: (1) inconsistent timestamp parsing — Activity built `"… …Z"` vs Home's ISO `"…T…Z"`, now one canonical parse; (2) Home's "Actions logged" tile used `entries.length` (capped at the page size ⇒ under-counted busy sites) — now uses the endpoint's real `total`, and Home fetches only `per_page=6`. `npm run build` green; new module + Activity lint-clean (one pre-existing prettier nit in Home untouched).

2026-07-12 — **[#36](https://github.com/plugpressco/saddle/issues/36) legible connection failures (revoked key vs stripped header)** — code done in working tree, not yet committed:
- `Saddle_MCP::authenticated()` now returns two distinct 401s instead of one generic `saddle_not_authenticated`: `saddle_credential_rejected` (reason `credential_rejected`, "reconnect the app") when Basic credentials reached PHP, vs `saddle_no_credentials` (reason `no_credentials`, "your host may be stripping the Authorization header — run the connection check") when none did.
- New `Saddle_Connection::request_carried_credentials()` (PHP_AUTH_USER / raw Basic header) is the discriminator; `Saddle_Connection::explain_auth_error()` on `rest_authentication_errors` (priority 20, wired in `saddle.php`) relabels **core's** own app-password 401 to the same `saddle_credential_rejected` — but only for the MCP endpoint and only when credentials were present, since core short-circuits a rejected key before the route gate runs.
- Tests: +9 in `tests/connection-test.php` (both gate branches, `request_carried_credentials`, relabeler scope/passthrough). Suite **309 green**; changed `includes/` files + `saddle.php` lint clean.
- NOT committed: the pre-existing uncommitted inputSchema `properties []→{}` normalization still shares `class-saddle-mcp.php` with this change — untangle before committing (commit #36 separately). Working tree also still on `main`.

2026-07-11 — **Admin UI refresh + two DS crash fixes + #8 hygiene → PR [#37](https://github.com/plugpressco/saddle/pull/37)** (branch `feat/admin-header`, base main):
- **[#35](https://github.com/plugpressco/saddle/issues/35) shipped** — full-bleed header was already in place; added Home `StatCard`/`StatGrid` tiles (Connected apps · Access level · Actions logged), `SkipLink` → `#pp-main`, dropped the redundant "%d apps" badge. Resynced the stale `@plugpress/ui` (node_modules was 0.6.2 vs the v0.8.0 pin).
- **Two `@plugpress/ui` bugs found via live testing, fixed at root + released:** **v0.8.1** — `Button asChild` threw `React.Children.only` on every use (passed a 2-entry `[spinner, children]` array to `Slot`); **v0.8.2** — `Dialog` "no accessible name" warning fired on *closed* dialogs (ConfirmProvider's idle dialog), now gated on `open`. Both tagged/pushed on `plugpressco/plugpress-ui` (manual release, not `fleet release`, to avoid repinning the whole fleet). Saddle also switched TopBar Docs/Rate to `Button href=`; pin now **v0.8.2**.
- **[#8](https://github.com/plugpressco/saddle/issues/8) closed** — deduped the four `log()` wrappers into `Saddle_Log::record_action()`; confirmed `--scope user` fix ships + connect/revoke tests already automated (22 green). Optional 401-legibility tail split to **[#36](https://github.com/plugpressco/saddle/issues/36)**.
- Note: an unrelated in-progress change to `includes/class-saddle-mcp.php` (MCP inputSchema `properties: []` → `{}` normalization) sits uncommitted in the working tree — NOT part of PR #37.

2026-07-10 (later session) — **Admin UI fully migrated to @plugpress/ui v0.6.0** (on top of v0.9.0):
- Pin bumped v0.2.0 → v0.6.0 (v0.6.0 verified non-breaking for Saddle: all 34 imports resolve, flat Tabs/FilterTabs/Steps + all `pp-*` classes survive; adds a WCAG 2.2 AA pass); `TooltipProvider`/`ConfirmProvider`/`Toaster` mounted once in App.jsx.
- Every screen rewritten on DS components: TopBar (Tabs/StatusDot), Onboarding + Permissions (CardRadioGroup/SelectableCard, ApplyBar, toast), Home (Hero/CalloutCard/CardGrid/RowList), Connect tab (PageHeader/EmptyState/RowList/useConfirm/Snippet/Badge), ConnectionHealth (CodeBlock/CalloutCard), ConnectWizard (Steps/CodeBlock/Snippet/LiveIndicator/useCopy — everCopied gate + back-out revoke preserved), Guidance/Memory (Card/RowList/Switch/Field/useConfirm/toast), Activity (FilterTabs/EmptyState/Badge).
- `admin/src/ui.jsx` compat shim **deleted**; zero `@wordpress/components` usage (PHP fallback deps + stylesheet deps updated to match); dead theme icons removed.
- `style.scss` 2,203 → **1,109 lines** (kept: token aliases, setup shell, lanes/chips, activity timeline, wizard flourishes, `.saddle-doc`).
- **Brand mark single-sourced**: `assets/brand/mark.svg` is the only copy — React `<BrandMark/>` (SVGR) and the PHP menu icon (file read + recolor, with fallback) both consume it.
- Docs reconciled: CLAUDE.md convention now names @plugpress/ui; DESIGN-ALIGNMENT.md re-decided (2026-07-10) — monochrome stands via the saddle accent, light-only.
- Earlier same day: the Closed-Loop Quality Engine free half (epic #22, PRs #28–#32, 300 green) — see git history.

## Next up
- **Mark Roach — reply drafted, awaiting Fahim's send. Two retractions in it.** (1) The MCP-Adapter diagnosis was WRONG: his own report says `Transport: Saddle built-in JSON-RPC`, so he does not have that plugin and rc6's headline fix is not his fix. (2) The trace shows ChatGPT's client (`Python/3.12 aiohttp/3.13.5`) refused **401 at the transport gate**, twice, with no `initialize` and no `tools/list` in the window — so this is authentication, full stop, not discovery, schemas, tiers or the handshake. Ask him for the **one** thing that closes it: run the Bearer curl probe below, then re-read Client traffic. rc7 now shows `auth:` and `scheme:` on every row, so the answer is readable without another round trip.
  ```bash
  curl -sS -o /dev/null -w '%{http_code}\n' -X POST https://staging.kesuk.net/wp-json/saddle/v1/mcp \
    -H 'Content-Type: application/json' -H 'Authorization: Bearer saddle-probe-not-a-real-token' \
    -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}'
  ```
  `scheme:bearer` on that row → the header arrives, ChatGPT is not sending its token. `scheme:none` → 20i is stripping Bearer, and Connections → Connection details & health will now say so itself and offer the one-click rule. **Still ask him to rotate the mrr.org.uk credentials he emailed in plaintext.**
- **rc7 is LIVE on the self-hosted channel** — `/v1/update` returns 1.0.0-rc7 with a signed package, and the zip served through the live link is byte-identical to the local build (sha256 `8ea73d41…`, 651,504 bytes) and **contains the updater** (verified by unzipping what the URL returns, not what was uploaded). First rc to actually reach anyone since rc5.
- **Confirm which zip Mark installed.** `class-saddle-updater.php` ships only in `-selfhosted`; if he has the `.org` zip his site never checks for updates and rc7 will not reach him either. One more manual install and then never again.
- **The bearer probe is stubbed, not field-proven.** `test_self_check_catches_a_bearer_stripped_while_basic_survives` drives a loopback that answers differently per scheme. Mark's site is the live test. If his report says `scheme:bearer`, the probe is correct and the problem is OpenAI's side of the token exchange — record that here either way.
- **Migrate saddle-pro and mailyard onto `saddle_context_sections`.** Free's half shipped in #102 and both still append through the old string filter, so Pro's `##` heading and Mailyard's floating "For email delivery problems:" sentence are still in the live context. Pro's tree currently has uncommitted release prep in it (a 1.4.0 → 1.4.1 bump), which is why it was left alone. Nothing breaks meanwhile — the compat path is tested.
- **The three surviving branches are all superseded or dead — decide and delete.** `feat/admin-header` (9 commits) and `feat/ui-plugpress-ui-v0.6.0` (a strict subset of it) targeted `@plugpress/ui` v0.8.2; `main` is on **v0.11.3**, TopBar and `Saddle_Log::record_action()` are both there, and every file they add exists on main in restructured form. `feat/freemius-parent` puts Freemius in FREE, which contradicts both CLAUDE.md ("no licensing or upsell in free") and the decision that free ships on WP.org with no Freemius.
- **Delete the v1.1.0 GitHub release + tag (one command):** `gh release delete v1.1.0 --repo plugpressco/saddle --cleanup-tag --yes && git tag -d v1.1.0`. Still outstanding, and #69 tried to reintroduce 1.1.0 this session — reverted in four places, including a `Stable tag` that was not a conflict and would have shipped silently.
- **Run `php scripts/revendor-wp-mcp.php --check` before any release.** New pre-release guard: it exits non-zero if the vendored library was refreshed without re-applying Saddle's text domain.
- **SUBMIT to WordPress.org.** Everything code-side is done and this session removed one of the reviewer-facing surfaces (#86). Fahim's three steps are in `WPORG-SUBMISSION.md`. Rebuild first: `npm run build && npx grunt build` — never `grunt release`, it bumps the version.
- **Live ChatGPT round-trip on plugpress.co** — still the real gate for the OAuth work, and now carrying three questions at once: the handshake fix against the client that exposed it, [#108](https://github.com/plugpressco/saddle/pull/108)'s level picker (set the existing connection to "Managing the site" on Connect, refresh the connector, expect ~160 tools and `saddle-waggle-update-seo-meta` present), and the plan question — if the write tools are *listed* and ChatGPT still declines to call them, that is the Business/Enterprise/Edu gate, not us. Record the answer in this file either way.
- **CI PHPUnit still red on GitHub Actions** (no WP core in the runner). Worth its own PR so future PRs get a real green; "green" currently means someone ran the suite locally and said so.

## Blockers / open questions
- Old work tickets #4–#6 remain from the previous scope — #5 (design-system unify) partially overlaps the shipped brief/bundle; triage against the new scope when convenient. (#8 closed 2026-07-11.)
